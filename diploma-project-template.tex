\documentclass[12pt,a4paper]{report}

\usepackage{cmap}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[utf8]{inputenc} % pentru suport diacritice
\usepackage[romanian]{babel} % setări pentru limba română 
\renewcommand\familydefault{\sfdefault} % sans serif
\addto\captionsromanian{\renewcommand{\tablename}{Tabelul}}

\usepackage[margin=2.54cm]{geometry}	% dimensiuni pagină și margini
\usepackage{graphicx} % support the \includegraphics command and options

% formatting sections and subsections
\usepackage{textcase}
\usepackage[titletoc, title]{appendix}
\usepackage{titlesec}
\titleformat{\chapter}{\large\bfseries\MakeUppercase}{\thechapter}{2ex}{}
\titlespacing*{\chapter}{0pt}{-29.5pt}{20pt}
\titleformat*{\section}{\large\bfseries}
\titleformat*{\subsection}{\large\bfseries}
\titleformat*{\subsubsection}{\normalsize}

\usepackage{chngcntr}
\counterwithout{figure}{chapter} % no chapter number in figure labels
\counterwithout{table}{chapter} % no chapter number in table labels
\counterwithout{equation}{chapter} % no chapter number in equation labels

\usepackage{booktabs} % for much better looking tables
\usepackage[section]{placeins} % don't let tables/figures float past a \section boundary
\usepackage{url} % Useful for inserting web links nicely
\usepackage[bookmarks,unicode,hidelinks]{hyperref}

\usepackage{array} % for better arrays (eg matrices) in maths
\usepackage{paralist} % very flexible & customisable lists (eg. enumerate/itemize, etc.)
\usepackage{verbatim} % adds environment for commenting out blocks of text & for better verbatim
\usepackage{subfig} % make it possible to include more than one captioned figure/table in a single float
\usepackage{enumitem}
\usepackage{tikz}
\usepackage{pdflscape}
\usepackage{listings}
\usepackage{longtable}
\setlist{noitemsep}

%%% HEADERS & FOOTERS
\usepackage{fancyhdr}
\pagestyle{empty}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\lhead{}\chead{}\rhead{}
\lfoot{}\cfoot{\thepage}\rfoot{}



\newcommand{\HeaderLineSpace}{0.1cm}
\newcommand{\UniTextRO}{UNIVERSITATEA NAȚIONALĂ DE ȘTIINȚĂ ȘI TEHNOLOGIE POLITEHNICA DIN BUCUREȘTI \\[\HeaderLineSpace] 
FACULTATEA DE AUTOMATICĂ ȘI CALCULATOARE \\[\HeaderLineSpace]
DEPARTAMENTUL DE CALCULATOARE\\}
\newcommand{\DiplomaRO}{PROIECT DE DIPLOMĂ}
\newcommand{\AdvisorRO}{Coordonator științific:}
\newcommand{\BucRO}{BUCUREȘTI}

\newcommand{\UniTextEN}{NATIONAL UNIVERSITY OF SCIENCE AND TECHNOLOGY POLITEHNICA BUCHAREST \\[\HeaderLineSpace]
FACULTY OF AUTOMATIC CONTROL AND COMPUTERS \\[\HeaderLineSpace]
COMPUTER SCIENCE AND ENGINEERING DEPARTMENT\\}
\newcommand{\DiplomaEN}{DIPLOMA PROJECT}
\newcommand{\AdvisorEN}{Thesis advisor:}
\newcommand{\BucEN}{BUCHAREST}

\newcommand{\frontPage}[6]{
\begin{titlepage}
\begin{center}
{\Large #1}  % header (university, faculty, department)
\vspace{50pt}
\begin{tabular}{p{6cm}p{4cm}}
\includegraphics[scale=0.4]{pics/upb-logo-resized.png} &
	\includegraphics[scale=0.5,trim={14cm 11cm 2cm 5cm},clip=true]{pics/cs-logo.pdf}
\end{tabular}

\vspace{105pt}
{\Huge #2}\\                           % diploma project text
\vspace{40pt}
{\Large #3}\\ \vspace{0pt}  % project title
% {\Large #4}\\                          % project subtitle
\vspace{40pt}
{\LARGE \Name}\\                   % student name
\end{center}
\vspace{50pt}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}p{6cm}r}
&{\large\textbf{#5}}\vspace{10pt}\\      % scientific advisor
&{\large \Advisor}                                    % advisor name
\end{tabular*}
\vspace{20pt}
\begin{center}
{\large\textbf{#6}}\\                                % bucharest
\vspace{0pt}
{\normalsize \Year}
\end{center}
\end{titlepage}
}

\newcommand{\frontPageRO}{\frontPage{\UniTextRO}{\DiplomaRO}{\ProjectTitleRO}{\ProjectSubtitleRO}{\AdvisorRO}{\BucRO}}
\newcommand{\frontPageEN}{\frontPage{\UniTextEN}{\DiplomaEN}{\ProjectTitleEN}{\ProjectSubtitleEN}{\AdvisorEN}{\BucEN}}

\linespread{1.2}
\setlength\parindent{0pt}
\setlength\parskip{.28cm}
\raggedbottom

%% Abstract macro
\newcommand{\AbstractPage}{
\begin{titlepage}
\textbf{\large SINOPSIS}\par
\AbstractRO\par\vfill
\textbf{\large ABSTRACT}\par
\AbstractEN \vfill
\end{titlepage}
}

%% Thank you macro
\newcommand{\ThanksPage}{
\begin{titlepage}
{\noindent \large\textbf{MULȚUMIRI}}\\
\Thanks
\end{titlepage}
}



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%   
%%
%%          End of template definitions
%%   
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


%%% Puteți elimina aceste linii din lucrare, servesc numai pentru template.
\newcommand{\worktype}[1]{[\textit{#1}] }
\newcommand{\dezvoltare}{\worktype{Dezvoltare de produs}}
\newcommand{\cercetare}{\worktype{Cercetare}}
\newcommand{\ambele}{\worktype{Ambele}}
%%%


%%
%%   Campurile de mai jos trebuie modificate de autor. Modificati doar continutul, nu si numele fiecarei definitii
%%
\newcommand{\ProjectTitleRO}{CultureQuest - Platforma mobilă de explorare urbană bazată pe proximitate, cu recomandări personalizate prin învățare federată}
\newcommand{\ProjectSubtitleRO}{Subtitlu (ex: versiunea 2018)}
\newcommand{\ProjectTitleEN}{CultureQuest - Proximity-Based Mobile Platform for Urban Exploration with Personalized Recommendations via Federated Learning}
\newcommand{\ProjectSubtitleEN}{Subtitle (eg: 2018 version)}
\newcommand{\Name}{Andrei Cozma}
\newcommand{\Advisor}{\shortstack[r]{Prof. dr. ing. Ciprian-Mihai Dobre \\ Prof. dr. ing. Radu-Ioan Ciobanu}}
\newcommand{\Year}{2026}

% Setări document
\title{Proiect de diplomă}
\author{\Name}
\date{\Year}

%%
%%   Campurile aferente rezumatului
%%
\newcommand{\AbstractRO}{CultureQuest este o platformă mobilă de explorare urbană bazată pe proximitate, oferind recomandări personalizate legate de obiectivele turistice și recreative dintr-un oraș, fără ca datele utilizatorului să părăsească dispozitivul. Problema pe care o tratez este că aplicațiile de tip \textit{city-guide} fie oferă aceleași sugestii tuturor, indiferent de interese, fie personalizează recomandările prin salvarea centralizată
a istoricului de interacțiuni cu obiectivele sau monitorizarea traseului, ceea ce ridică probleme din punctul de vedere al confidențialității. Pentru a evita acest compromis, am implementat o aplicație Flutter în care datele sunt salvate doar local, interacțiunile vizibile altor utilizatori precum recenziile sunt anonime și adăugate strict de vizitatori, iar rutele turistice generate sunt individualizate prin intermediul unei rețele neurale de tip
perceptron multi-strat (MLP, \textit{Multi-Layer Perceptron}). Modelul global inițial este pre-antrenat pe seturi de date publice, iar acesta este modificat și îmbunătățit prin tehnici de învățare federată (FL, \textit{Federated Learning}) rulate asincron pe dispozitivele clienților. Recomandările de rute combină scorul oferit de model pentru o atracție turistică cu proximitatea acesteia, iar un sistem de misiuni gamificate încurajează explorarea și
colectarea de noi date. Printr-o simulare care include mai multe runde - mai exact 600 - se confirmă funcționarea fluxului FL în mod corespunzător.}

\newcommand{\AbstractEN}{CultureQuest is a proximity-based mobile urban exploration platform, offering personalized recommendations for tourist and recreational attractions in a city, without the user's data leaving the device. The problem I address is that city-guide type applications either offer the same suggestions to all users, regardless of interests, or personalize recommendations by centrally storing a user's interaction history with
landmarks or by tracking their route, both of which raise privacy concerns. To avoid this trade-off, I developed a Flutter application where data is saved only locally, interactions visible to other users such as reviews are anonymous and can only be added by actual visitors, and the generated tourist routes are personalized through a multi-layer perceptron (MLP) neural network. The initial global model is pre-trained on public datasets,
and it is modified and improved through federated learning (FL) techniques run asynchronously on the client devices. Route recommendations combine the model's score for a tourist attraction with its proximity, while a gamified quest system encourages exploration and data collection. A simulation that includes multiple client rounds - 600, to be exact - confirms that the FL flow is working properly.}

%%
%%   Campurile aferente paginii de multumiri
%%
\newcommand{\Thanks}{(opțional) Aici puteți introduce o secțiunea specială de mulțumiri / acknowledgments. }

\begin{document}

\frontPageRO
\frontPageEN

\begingroup
\linespread{1}
\tableofcontents
\endgroup

\AbstractPage

% poate fi comentata sau stearsa
% \ThanksPage


% Textul licentei incepe de aici 



\chapter{Introducere}\pagestyle{fancy}

Acest capitol prezintă contextul în care se situează proiectul și problema care îl motivează, obiectivele propuse, o privire de ansamblu asupra soluției dezvoltate și a rezultatelor obținute, precum și modul în care este organizat restul lucrării.

\section{Context}

Telefoanele mobile au devenit, în ultimii ani, principalul instrument prin care utilizatorii interacționează cu mediul din jurul lor, iar aplicațiile mobile bazate pe locație - de la Google Maps, TripAdvisor și GetYourGuide, la diverse aplicații de tip \textit{city-guide} - au devenit una dintre principalele modalități prin care turiștii și localnicii descoperă obiective turistice și culturale dintr-un oraș și primesc recomandări din ce în ce mai adaptate propriilor interese \cite{gavalas2014mobile}, tendință susținută de creșterea constantă a pieței, cu peste 850 de milioane de utilizatori de aplicații de călătorit la nivel mondial în 2023\footnote{Business of Apps, \textit{Travel App Report 2025}. \url{https://www.businessofapps.com/data/travel-app-report/} (accesat 19.06.2026).}.

Pentru a oferi recomandări personalizate, aceste aplicații se bazează pe colectarea și procesarea centralizată a istoricului de interacțiuni - locurile vizitate și în ce ordine, recenziile adăugate, timpul petrecut în diverse locații, ce categorii culturale îl atrag - pentru a înțelege comportamentul concret al utilizatorului. Astfel, accesul la acest ansamblu de date, coroborat cu accesul la poziția GPS în orice moment, necesară pentru \textit{geofencing} și generarea de trasee, înseamnă că un server centralizat ar acumula un profil detaliat al fiecărui turist, care ar putea dezvălui rutine zilnice, preferințe personale sau chiar afilierea religioasă și orientările politice \cite{kairouz2021advances}.

Învățarea federată (FL, \textit{Federated Learning}) apare ca alternativă la acest model centralizat: modelul este antrenat local, pe dispozitivul utilizatorului, folosind datele acestuia, iar către server sunt trimise doar actualizările rezultate, nu datele brute, pentru a fi agregate într-un model global. FL este deja folosită la scară destul de mare în sisteme de producție \cite{bonawitz2019towards,zhang2021survey,li2020applications}, ceea ce motivează aplicarea ei și în cadrul acestei platforme, pentru a oferi recomandări de rute turistice fără a compromite confidențialitatea clienților.

\section{Definirea problemei}

Majoritatea aplicațiilor de explorare urbană dezvoltate oferă, în absența unui profil construit din acțiunile turiștilor pe platformă, aceleași rute tuturor, indiferent de interesele fiecăruia, chiar dacă acestea sunt legate de artă, gastronomie, natură sau alte evenimente culturale. Această abordare generică este mai simplă de dezvoltat și de întreținut, însă, nu ține cont de faptul că două persoane care vizitează același oraș pot avea preferințe complet diferite în ceea ce privește modul în care doresc să îl exploreze.

Varianta prin care o aplicație devine personalizată presupune, de regulă, ca istoricul de interacțiuni al utilizatorului să fie încărcat și procesat pe un server central, exact tipul de practică care aduce în discuție problemele de confidențialitate menționate mai sus. Utilizatorului i se prezintă, astfel, un compromis: fie acceptă recomandări generice, fie acceptă ca datele sale comportamentale să fie colectate central pentru a primi recomandări adaptate intereselor sale - iar acest compromis este punctul de plecare al proiectului de față.

\section{Obiective}

Pornind de la acest compromis, proiectul de față își propune să arate că personalizarea și păstrarea confidențialității nu se exclud reciproc, prin trei obiective principale:

\begin{enumerate}[label=(\alph*)]
\item O aplicație mobilă prin care utilizatorul poate descoperi obiective culturale dintr-un oraș printr-o explorare bazată pe proximitate, cu un sistem de misiuni (\textit{quest-uri}) și puncte obținute pentru interacțiunile pe platformă, care îl încurajează să viziteze locuri noi.

\item Personalizarea recomandărilor de rute pentru fiecare utilizator, fără ca datele acestuia - istoricul de interacțiuni, locațiile vizitate - să părăsească dispozitivul.

\item Implementarea unui flux complet de FL: pre-antrenarea modelului global pe date publice și ajustarea fină (\textit{fine-tuning}); antrenarea locală prin coborâre pe gradient stocastică (SGD, \textit{Stochastic Gradient Descent}) conform FedAvg~\cite{mcmahan2017fedavg}, trimiterea spre server doar a variației ponderilor față de modelul global, agregarea asincronă a actualizărilor prin FedAsync~\cite{xie2019fedasync} și împiedicarea deducerii din acestea a interacțiunilor concrete, prin adăugarea de zgomot Gaussian asupra variației ponderilor.
\end{enumerate}

\section{Soluția propusă}

Soluția dezvoltată este o aplicație mobilă realizată în Flutter, prin care utilizatorul poate vedea pe hartă obiectivele turistice și evenimentele din apropiere. Pe lângă explorare, aplicația oferă un sistem de \textit{quest-uri}, care încurajează vizitarea unor obiective noi, dar și posibilitatea de a lăsa recenzii, povești sau chiar alte \textit{quest-uri} pentru locurile vizitate - tot ce adaugă utilizatorii devine, în timp, parte din imaginea pe care comunitatea o are despre fiecare atracție.

Pentru personalizare, fiecare utilizator are pe dispozitiv un model mic, un perceptron multi-strat (MLP, \textit{Multi-Layer Perceptron}), care reprezintă un scor de recomandare pentru fiecare obiectiv. Pe baza acestui scor, fiecărui obiectiv i se asociază o etichetă care arată cât de recomandat este pentru utilizator, în funcție de interesele acestuia, de ora curentă, de tipul locului etc., iar la generarea rutei, scorul fiecărui obiectiv este combinat cu distanța față de poziția curentă. Modelul global inițial este pre-antrenat cu ajutorul unor seturi de date din mediul online, apoi antrenat pe dispozitiv prin FL, iar \textit{backend-ul} coordonează învățarea asincronă.

\section{Rezultatele obținute}

La nivel general, rezultatul este o aplicație Flutter funcțională, care reunește harta cu obiective și evenimente din apropiere, generarea de rute personalizate și sistemul de \textit{quest-uri}. Personalizarea descrisă în secțiunea anterioară este vizibilă direct în aplicație, pentru fiecare obiectiv din apropiere și în cadrul rutelor generate.

Pe partea de FL, modelul de pe dispozitiv pornește de la o variantă a modelului global trecută și prin etapa de \textit{fine-tuning}, iar antrenarea locală se face prin FedAvg. Agregarea pe server este asincronă, cu o reducere în funcție de vechimea (\textit{staleness discount}) actualizărilor venite de la clienți \cite{xie2019fedasync} - o metodă prin care influența lor asupra modelului global este diminuată. O simulare de 600 de runde cu 10.000 de clienți sintetici arată o reducere a pierderii BCE (\textit{Binary Cross-Entropy}) cu 30,9\% (de la 0,695 la 0,480), față de 27,2\% în varianta fără discount. Chiar dacă diferența procentuală nu pare foarte semnificativă, simularea a inclus doar 20\% clienți cu \textit{staleness-ul} mai mare decât 2, deci raportarea se face la acest procent. Am confirmat, astfel, că ignorarea vechimii actualizărilor contribuie la degradarea convergenței. În plus, eficiența rulării pe dispozitiv este validată de latența de inferență a modelului: $\approx$40 µs pentru fiecare predicție în simularea NumPy, fără dependențe native adiționale, ceea ce permite personalizarea continuă fără impact perceptibil asupra experienței.

\section{Structura lucrării}

\textbf{Capitolul 1} a introdus, la nivel general, contextul, problema, obiectivele, soluția propusă și rezultatele obținute. Capitolele următoare intră în detaliu pe fiecare dintre aceste aspecte.

\textbf{Capitolul 2} stabilește cerințele aplicației, pornind de la cazurile de utilizare ale unui turist sau localnic care explorează un oraș, și le împarte în cerințe funcționale și non-funcționale. Tot aici se delimitează și domeniul de aplicare al proiectului.

\textbf{Capitolul 3} trece în revistă aplicațiile de explorare urbană existente și modul în care acestea personalizează recomandările, comparând abordările centralizate cu FL. Se poziționează CultureQuest pe baza taxonomiei FL, se explică de ce alte alternative de algoritmi FL nu se potrivesc design-ului asincron al platformei și se justifică tehnologiile alese.

\textbf{Capitolul 4} detaliază arhitectura propusă a sistemului. Aici se discută structura aplicației mobile și a serviciilor \textit{backend}, proiectarea modelului MLP și a procesului FL, fluxul de generare a rutelor, elementele de gamificare și arhitectura de confidențialitate.

După proiectare, \textbf{Capitolul 5} trece la implementarea efectivă - serviciul client FL și mecanismul de confidențialitate diferențială (DP, \textit{Differential Privacy}), serviciile \textit{backend} de agregare și de moderare a conținutului, etapa de pre-antrenare și \textit{framework-ul} folosit pentru simularea procesului FL.

\textbf{Capitolul 6} evaluează rezultatele obținute - corectitudinea funcțională a aplicației, performanța modelului rezultată din pre-antrenare și din fluxul FL, efectul DP și al limitării asupra robusteții - și include o discuție comparativă față de alternativele respinse.

\textbf{Capitolul 7} încheie lucrarea cu o privire de ansamblu asupra obiectivelor stabilite inițial, în raport cu ce a fost dezvoltat și propune direcții viitoare - de exemplu, personalizarea suplimentară per utilizator sau alte tipuri de agregare în ceea ce privește modelul rețelei neurale.

\chapter{Analiza Cerințelor}

Capitolul de față detaliază cerințele care reies din obiectivele stabilite anterior, și anume cine sunt utilizatorii și care sunt cazurile de utilizare care trebuie acoperite, ce funcționalități rezultă de aici, ce constrângeri non-funcționale se fac necesare și ce rămâne în afara domeniului de aplicare. Toate aceste cerințe stau la baza deciziilor de proiectare din Capitolul 4, fiind verificate ulterior în evaluarea din Capitolul 6.

\section{Utilizatori țintă și cazuri de utilizare}

Utilizatorul principal al aplicației este un turist sau un localnic care explorează un oraș - sau o zonă a acestuia - cu dorința de a vizita obiective turistice sau culturale din proximitate, adaptate intereselor proprii. Am identificat patru scenarii care acoperă principalele categorii de utilizatori: turistul aflat pentru prima oară într-un loc necunoscut, cu dorința de a se orienta ușor și de a descoperi obiective potrivite pentru el; localnicul interesat de evenimente și obiective culturale din apropiere, cu dorința de a descoperi conținut nou și de a contribui la comunitate; utilizatorul interesat de confidențialitate, vrând să beneficieze de recomandări personalizate fără ca datele sale să ajungă într-un loc centralizat; administratorul, cu rolul de a valida propunerile comunității și de a menține calitatea informațiilor pentru ceilalți utilizatori. Cerințele au fost validate prin corelarea fiecărei funcționalități cu unul sau mai multe dintre aceste scenarii.

Primul caz de utilizare este cel de generare de rute și suport pe parcursul urmării acestora, în care utilizatorul vede pe hartă poziția sa curentă, locurile de interes din apropiere și primește o rută care combină preferințele sale cu acestea. Mai mult, interacțiunile cu aplicația - vizite, \textit{quest-uri} finalizate, recenzii - determină ajustarea, prin FL, a modelului de pe dispozitiv - și, implicit, și a celui global - fără ca istoricul brut al interacțiunilor să ajungă pe server. Un al doilea caz de utilizare este cel de contribuție de conținut, în care utilizatorul poate propune obiective noi, povești, \textit{quest-uri} sau recenzii, toate acestea necesitând aprobarea de către alte persoane care folosesc aplicația sau de către un administrator. Prin conturile de tip administrator, relevante mai ales pentru 2.2, se aprobă sau se respinge conținutul propus de utilizatorii obișnuiți.

\section{Cerințe funcționale}

Pornind de la cazurile de utilizare din secțiunea anterioară, cerințele funcționale au fost sintetizate în \textbf{tabelul~\ref{tab:cerinte-functionale}}, fiecare cu o descriere, condiții de acceptare și secțiunile din Capitolele 4-6 unde cerința este tratată. Primul grup (de la \textbf{FR-1} la \textbf{FR-4}) acoperă bucla principală de explorare: afișarea pe hartă a obiectivelor turistice din proximitate, generarea rutei care combină scorul modelului de tip MLP cu distanța față de poziția curentă, \textit{quest-urile} disponibile la obiectivele vizitate și posibilitatea de a adăuga sau vizualiza recenzii.

Al doilea grup (de la \textbf{FR-5} la \textbf{FR-7}) ține de personalizare și administrare: \textbf{FR-5} descrie fluxul procesului FL: primirea parametrilor modelului actualizat de la server, urmată de antrenarea locală, fără ca istoricul interacțiunilor să ajungă pe server, flux verificat prin simularea de 600 de runde descrisă în 6.2; \textbf{FR-6} descrie controalele de confidențialitate - asigurarea DP, vizibilitatea asupra datelor - prin adăugarea de zgomot Gaussian și limitare a gradienților aplicate pe actualizările de ponderi, verificat prin analiza compromisului dintre confidențialitate și utilitate din 6.3; iar \textbf{FR-7} descrie rolul de administrator - aprobarea sau respingerea conținutului propus, verificat prin demo funcțional în 6.1.

\begin{table}[h]
\centering
\scriptsize
\caption{Cerințe funcționale cu condiții de acceptare.}
\label{tab:cerinte-functionale}
\begin{tabular}{|l|p{2.8cm}|p{8cm}|p{1.8cm}|}
\hline
\textbf{ID} & \textbf{Descriere} & \textbf{Condiții de acceptare} & \textbf{Secțiune} \\
\hline
FR-1 & Obiective din proximitate pe hartă. &
\textit{I:} coordonate GPS. \textit{E:} markere pentru obiective aflate la sub 200\,m. \textit{Î:} toate obiectivele din raza setată apar pe hartă. \textit{T:} demo funcțional. & 4.7, 5.1.4, 6.1 \\
\hline
FR-2 & Rute personalizate: scor MLP și proximitate. &
\textit{I:} interese + poziție curentă. \textit{E:} rută ordonată după scor MLP ponderat cu distanța. \textit{Î:} rute diferite pentru utilizatori diferiți. \textit{T:} demo cu utilizatori diferiți. & 4.5, 5.1.5, 6.1 \\
\hline
FR-3 & \textit{Quest-uri} la obiective vizitate; puncte și progres. &
\textit{I:} intrare în \textit{geofence-ul} unui obiectiv. \textit{E:} \textit{quest} disponibil; puncte la finalizare. \textit{Î:} \textit{quest} activat la intrare; puncte actualizate la final. \textit{T:} demo funcțional. & 4.6, 5.1.6, 6.1 \\
\hline
FR-4 & Adăugare și vizualizare recenzii. &
\textit{I:} text + rating de la utilizator autentificat. \textit{E:} recenzie vizibilă în cadrul obiectivului. \textit{Î:} recenzia apare pentru toți utilizatorii. \textit{T:} demo funcțional. & 5.2.3, 6.1 \\
\hline
FR-5 & Sincronizare model și antrenare locală, fără date pe server. &
\textit{I:} interacțiuni locale. \textit{E:} ponderi actualizate local; doar actualizarea trimisă la server. \textit{Î:} pierderea BCE scade cu $\geq$25\% față de varianta inițială. \textit{T:} simulare FL, 600 runde, 10.000 clienți. & 4.4, 5.1.1, 5.1.2, 5.1.3, 5.2.1, 6.2 \\
\hline
FR-6 & Zgomot DP pe actualizări; vizibilitate asupra datelor trimise. &
\textit{I:} actualizare locală a ponderilor. \textit{E:} actualizare cu zgomot Gaussian și limitare a normei. \textit{Î:} interacțiunile utilizatorului nu sunt transmise direct. \textit{T:} compromis confidențialitate, utilitate. & 4.8, 5.1.2, 6.3 \\
\hline
FR-7 & Administrator: aprobă sau respinge conținut. &
\textit{I:} propunere de conținut. \textit{E:} publicat sau respins de administrator. \textit{Î:} conținut neaprobat absent din aplicație. \textit{T:} demo flux administrator. & 5.2.3, 6.1 \\
\hline
\multicolumn{4}{l}{\textit{I}~=~Intrare; \textit{E}~=~Ieșire; \textit{Î}~=~Îndeplinit; \textit{T}~=~Testat în Cap.~6.} \\
\end{tabular}
\end{table}

\section{Cerințe non-funcționale}

La fel ca în secțiunea 2.2, cerințele non-funcționale au fost sintetizate în \textbf{tabelul~\ref{tab:cerinte-nonfunctionale}}, cu aceeași structură (descriere și secțiunile din Capitolele 4-6 unde sunt tratate), fiecare însoțită de o metrică verificabilă. Primul grup (de la \textbf{NFR-1} la \textbf{NFR-3}) are legătură cu constrângerile impuse pe dispozitiv: \textbf{NFR-1} cere ca nicio informație privată despre utilizator - exceptând lista de interese - să nu părăsească telefonul, \textbf{NFR-2} cere ca obiectivele turistice să rămână disponibile și fără conexiune la internet, iar \textbf{NFR-3} cere ca antrenarea locală să nu fie complexă din punct de vedere computațional - ceea ce a motivat alegerea unui model MLP de dimensiuni reduse, antrenat în 5 epoci locale.

Al doilea grup ține de partea de \textit{backend}: \textbf{NFR-4} impune ca serviciul de agregare să suporte actualizări asincrone primite de la mai mulți clienți, fără conflicte între runde, asigurat printr-un \textit{lock} Redis, iar \textbf{NFR-5} impune ca utilizatorii să primească recomandări utile încă de la început printr-un model global pre-antrenat \cite{bonawitz2019towards}, și din caracteristici bazate pe ariile de interes asociate fiecărui obiectiv, până când procesul FL își face efectul.

\begin{table}[h]
\centering
\scriptsize
\caption{Cerințe non-funcționale.}
\label{tab:cerinte-nonfunctionale}
\begin{tabular}{|l|p{10cm}|p{2.5cm}|}
\hline
\textbf{ID} & \textbf{Descriere} & \textbf{Secțiune} \\
\hline
NFR-1 & Confidențialitate: nicio interacțiune a utilizatorului nu părăsește dispozitivul; doar actualizarea de ponderi ($\approx$5 KB la fiecare rundă FL) este trimisă la server. & 4.8, 5.1.2, 5.1.3, 6.3 \\
\hline
NFR-2 & Reziliență offline: obiective turistice stocate în \textit{cache} local; accesibile fără conexiune la rețea. & 5.1.4, 6.1 \\
\hline
NFR-3 & Cost redus de antrenare pe dispozitiv: model de 1.281 parametri ($\approx$5 KB valori în format float32); 5 epoci locale la fiecare rundă FL; calculul scorului pentru un obiectiv (un singur \textit{forward pass}) durează $\approx$39\,µs (simulare pe laptop). & 4.4.1, 5.1.1, 6.2.1 \\
\hline
NFR-4 & Scalabilitatea \textit{backend-ului}: un \textit{lock} Redis serializează actualizările FL asincrone fără conflicte de date; 0 erori la cel mult 10 cereri simultane (latență medie 1,4 s la 10 cereri); degradare la 20 cereri. & 4.4.6, 5.2.1, 6.3 \\
\hline
NFR-5 & \textit{Cold start}: recomandări utile pentru utilizatori folosind modelul pre-antrenat, fără runde FL prealabile; FL îmbunătățește modelul (pierdere BCE: 0,695 la start se transformă în 0,480 după 600 runde). & 4.4.1, 5.3, 6.2.1, 6.2.2 \\
\hline
\end{tabular}
\end{table}

\section{Domeniul de aplicare al proiectului}

Cerințele funcționale și non-funcționale stabilite anterior delimitează domeniul de aplicare al proiectului: aplicația mobilă Flutter cu suport Android și iOS cu recomandări de rute turistice, pornind de la pre-antrenarea globală și fiind îmbunătățite prin FL (simulat pe date sintetice cu obiective turistice din București); moderarea conținutului generat de turiști și localnici prin vot; autentificarea și gestionarea celor două tipuri de conturi (administrator și utilizator simplu). Rămâne în afara domeniului de aplicare personalizarea suplimentară la nivel de utilizator prin straturi separate ale modelului MLP (\textit{personal head}), care nu ar fi trimise către server, rezultând într-o arhitectură de tip FedPer \cite{arivazhagan2019fedper} - propusă ca direcție de lucrări viitoare, dar neimplementată în versiunea curentă. În plus, în afara domeniului se află și securitatea avansată la nivel de rețea, agregarea securizată (SecAgg \cite{bonawitz2017secagg}), incompatibilă cu FL-ul asincron utilizat, și validarea în producție și colectarea de date reale de interacțiune.

Având stabilite cerințele și limitele proiectului, Capitolul 3 trece în revistă soluțiile existente pentru explorare urbană (Google Maps, TripAdvisor, alte aplicații de tip \textit{city-guide}) și realizează o analiză comparativă între abordările centralizate și cele bazate pe FL pentru recomandări.

\chapter{Soluții Existente}

Înainte de proiectarea propriu-zisă a soluției, acest capitol analizează contextul în care se înscrie CultureQuest. Aplicațiile de explorare urbană existente oferă un grad bun de personalizare a recomandărilor, însă, acestea se bazează pe sisteme centralizate de colectare a datelor, de unde și comparația cu abordările bazate pe FL și cu stadiul actual al cercetării din domeniu, folosit pentru a poziționa CultureQuest în cadrul soluțiilor FL. Pe baza acestei analize, capitolul explică de ce o parte dintre cele mai cunoscute variante de algoritmi FL nu se potrivesc design-ului asincron al platformei și justifică restul tehnologiilor folosite în implementare.

\section{Aplicații existente pentru explorare urbană}

Platformele mobile de recomandare pentru turiști \cite{gavalas2014mobile} oferă un anumit nivel de personalizare bazat pe istoricul de activitate al utilizatorului, stocat și procesat pe serverele furnizorului. \textbf{Google Maps} generează, de exemplu, recomandări pe baza istoricului de locații, a căutărilor și a evaluărilor asociate contului Google\footnote{Google, pagina oficială despre recomandările personalizate. \url{https://support.google.com/websearch/answer/17025248?hl=en} (accesat 19.06.2026).}. \textbf{TripAdvisor} merge pe o logică asemănătoare, prin faptul că sugestiile de obiective și restaurante sunt adaptate pe baza recenziilor citite, a căutărilor anterioare și a locațiilor vizitate, tot prin contul de utilizator, procesat central\footnote{TripAdvisor, pagina oficială despre funcționarea recomandărilor/politica de confidențialitate. \url{https://tripadvisor.mediaroom.com/US-privacy-policy} (accesat 19.06.2026).}. În ambele cazuri, personalizarea funcționează doar pentru că acest istoric de interacțiuni ajunge să fie analizat pe server, nu pe dispozitivul utilizatorului.

Pe lângă aceste platforme axate pe navigare, există și aplicații specializate în activități turistice legate de rezervări, precum \textbf{GetYourGuide}, care recomandă tururi cu ghizi, bilete și experiențe pe baza destinației căutate și a istoricului de căutări sau rezervări al utilizatorului, tot stocat și procesat central\footnote{GetYourGuide, pagina oficială despre cum funcționează recomandările și politica de confidențialitate. \url{https://www.getyourguide.com/c/privacy-policy} (accesat 19.06.2026).}. Spre deosebire de Google Maps sau TripAdvisor, accentul este pus pe activități plătite și predefinite, nu pe explorarea liberă a orașului, așa că nu există rute generate dinamic în funcție de poziția utilizatorului. Dincolo de aceste diferențe, toate trei se bazează pe același principiu - ML centralizat, antrenat pe date de la un număr mare de utilizatori.

O categorie specială o reprezintă aplicațiile care propun tururi audio ghidate, precum \textbf{izi.TRAVEL}, care oferă narațiuni geolocalizate și trasee audio predefinite pentru obiective culturale, în special muzee, din diverse orașe. Față de Google Maps sau TripAdvisor, accentul cade pe conținutul cultural structurat, nu pe navigare sau rezervări, iar traseele sunt pregătite de muzee, ghizi sau organizații certificate, iar aplicația pornește automat narațiunea pe măsură ce utilizatorul ajunge în apropierea unui punct de interes\footnote{izi.TRAVEL, pagina oficială. \url{https://izi.travel/} (accesat 19.06.2026).}. Tururile pot fi descărcate pentru a funcționa în mod offline, dar lipsesc generarea dinamică de rute bazată pe profilul utilizatorului, personalizarea prin ML și gamificarea.

Gamificarea explorării bazate pe locația curentă este o componentă lipsă din toate aplicațiile prezentate mai sus, dar este acoperită parțial de \textbf{Geocaching}. Aici utilizatorii caută ascunzători fizice plasate de alți membri ai comunității, navighează la coordonatele GPS corespunzătoare și își înregistrează descoperirile. Ei pot strânge puncte și debloca realizări\footnote{Groundspeak Inc. (Geocaching HQ), termeni de utilizare și politică de confidențialitate. \url{https://www.geocaching.com/account/documents/privacypolicy} (accesat 19.06.2026).}. Cum conținutul este integral generat de utilizatori, Geocaching ajunge să fie cea mai apropiată alternativă pentru componenta de explorare gamificată. Cu toate acestea, recomandările personalizate tot nu sunt prezente nici în cadrul acestei aplicații, rutele nu sunt construite dinamic, partea de ML pentru recomandări lipsește și istoricul de activitate nu este stocat local, ci centralizat.

La polul opus se situează aplicațiile axate pe confidențialitate, precum \textbf{Organic Maps}, un proiect \textit{open-source} bazat pe date de la OpenStreetMap, fără cont de utilizator și fără colectare de date\footnote{Organic Maps, pagina oficială - „no tracking, no data collection”. \url{https://organicmaps.app/privacy/} (accesat 19.06.2026).}. Aici personalizarea nu există, iar toți utilizatorii dintr-o zonă văd același set de obiective, indiferent de interese, pentru că nu există niciun istoric din care acestea să poată fi accesate. Între cele două extreme - fie personalizare cu date colectate central, fie confidențialitate fără personalizare - se situează CultureQuest, unde scorul de personalizare este calculat prin FL, fără ca datele brute să părăsească dispozitivul. Toate aceste cazuri sunt adunate în \textbf{tabelul~\ref{tab:apps-comparativ}}.

\begin{table}[h]
\centering
\scriptsize
\caption{Comparație între aplicații existente de explorare urbană și CultureQuest.}
\label{tab:apps-comparativ}
\begin{tabular}{|p{1.9cm}|p{2.2cm}|p{2.2cm}|p{1.9cm}|p{1.4cm}|p{1.6cm}|p{1.4cm}|}
\hline
\textbf{Aplicație} & \textbf{Rute turistice} & \textbf{Personalizare} & \textbf{Fără date pe server} & \textbf{Suport offline} & \textbf{Cost / acces} & \textbf{Gamificare} \\
\hline
Google Maps & Navigație generală & Centralizată & Nu & Parțial & Gratuit / API plătit & Nu \\
\hline
TripAdvisor & Nu & Centralizată & Nu & Nu & Gratuit / API plătit & Limitat \\
\hline
GetYourGuide & Tururi predefinite & Limitată & Nu & Nu & Gratuit & Nu \\
\hline
izi.TRAVEL & Trasee audio predefinite & Nu & Nu & Parțial & Gratuit & Nu \\
\hline
Geocaching & Nu & Nu & Nu & Parțial & Freemium & Da \\
\hline
Organic Maps & Navigație generală & Nu & Da & Da & Gratuit, open source & Nu \\
\hline
CultureQuest & Dinamice, personalizate (FL) & Locală (FL) & Da & Da & Gratuit, open source & Da \\
\hline
\end{tabular}
\end{table}

\section{Analiză comparativă între abordările centralizate și învățarea federată pentru recomandări}

Sistemele de recomandare clasice se bazează, în general, pe filtrarea colaborativă, care recomandă unui utilizator ceea ce au apreciat alți utilizatori cu interese similare, și pe filtrarea bazată pe conținut, care recomandă obiective similare cu cele apreciate deja de același utilizator \cite{adomavicius2005recommender}. În ambele cazuri, determinarea similarităților are nevoie de o bază de date centralizată cu istoricul de activitate al clienților - exact modelul din spatele recomandărilor descrise în secțiunea anterioară pentru aplicațiile menționate. Avantajul este accesul la un volum mare de date eterogene, din care modelul învață tendințe generale utile chiar și pentru utilizatori noi, pe baza unor profiluri asemănătoare existente în sistem. Prețul acestui avantaj este, însă, tot cel discutat mai sus. Pentru ca modelul să poată face aceste calcule, istoricul de activitate al fiecărui utilizator trebuie să ajungă și să rămână pe server, sub controlul furnizorului.

FL schimbă direcția, astfel încât modelul ajunge la date, nu invers - antrenarea are loc local, pe dispozitivul fiecărui utilizator, iar pe server ajung doar actualizările rezultate ale modelului, nu istoricul de activitate în sine. Datele rămân astfel pe dispozitiv, iar confidențialitatea nu mai depinde de politicile furnizorului, ci de proiectarea protocolului de agregare. Diferența se vede cel mai clar la pornirea la rece (\textit{cold-start}). O aplicație aflată la început nu are încă un model antrenat suficient din cauza unui număr mic de persoane care o folosesc, așa că recomandările vin dintr-un model global pre-antrenat și din caracteristicile bazate pe conținut ale obiectivelor, la fel ca în cazul centralizat - personalizarea prin FL se construiește abia pe măsură ce utilizatorii interacționează cu aplicația.

\section{Învățare federată: Stadiul actual}

FL a apărut ca direcție de cercetare odată cu FedAvg \cite{mcmahan2017fedavg}, ca răspuns direct la problema discutată în secțiunea anterioară - cum poate fi antrenat un model pe datele mai multor clienți, fără ca acestea să fie centralizate pe un server. De atunci, domeniul s-a diversificat, acoperind variante de agregare, metode DP, robustețe la clienți adversariali și scenarii de personalizare \cite{kairouz2021advances}. Pentru a situa CultureQuest în acest cadru, ne putem ghida după două axe de clasificare devenite standard în literatură: tipul de FL, în funcție de cum sunt distribuite datele între clienți, și modul de desfășurare, în funcție de cine sunt clienții și care este comportamentul lor.

Prima axă, propusă de Yang et al. \cite{yang2019fml}, împarte FL în trei categorii:

\begin{itemize}
  \item \textbf{Orizontal (HFL)} - clienții au același spațiu de caracteristici, dar mulțimi diferite de exemple, fiind situația tipică atunci când același tip de date este colectat de la utilizatori diferiți.
  \item \textbf{Vertical (VFL)} - clienții au în comun o parte din exemple, dar caracteristici diferite, ca în cazul unor organizații care dețin date diferite despre aceiași utilizatori.
  \item \textbf{FL cu transfer (FTL)} - pentru situațiile în care nici exemplele, nici caracteristicile nu se suprapun semnificativ.
\end{itemize}

CultureQuest se încadrează clar în categoria HFL. Fiecare telefon antrenează același model, pe același spațiu de 22 de caracteristici asociate obiectivelor turistice, diferența dintre clienți fiind doar istoricul propriu de interacțiuni.

A doua axă împarte FL în două moduri de desfășurare \cite{kairouz2021advances, li2020challenges}:

\begin{itemize}
  \item \textbf{\textit{Cross-device}} - clienții sunt un număr mare de dispozitive individuale, cu conexiuni nesigure și resurse limitate, fiecare contribuind cu o cantitate mică de date.
  \item \textbf{\textit{Cross-silo}} - clienții sunt un număr mic de organizații sau servere, cu conexiuni stabile și seturi de date mari.
\end{itemize}

CultureQuest este, din această perspectivă, un sistem \textit{cross-device}. Telefoanele utilizatorilor sunt conectate la internet intermitent, fiecare cu un volum mic de interacțiuni locale. Pentru aplicație, s-a optat, așadar, pentru agregare asincronă \cite{xie2019fedasync}, tocmai pentru că acești clienți sunt intermitenți și nu este practic să se aștepte formarea unei cohorte complete la fiecare rundă de învățare. Este nevoie, prin urmare, și de mecanisme de robustețe la actualizări extreme \cite{lyu2020threats}. \textbf{Figura~\ref{fig:taxonomie-fl}} situează CultureQuest pe ambele axe.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  cell/.style={draw, minimum width=3.2cm, minimum height=1.3cm},
  hl/.style={draw, fill=gray!20, minimum width=3.2cm, minimum height=1.3cm, font=\bfseries, align=center}
]
  \node[font=\bfseries] at (1.6, 3.2) {HFL};
  \node[font=\bfseries] at (5.0, 3.2) {VFL};
  \node[font=\bfseries] at (8.4, 3.2) {FTL};

  \node[font=\bfseries, anchor=east] at (-0.2, 2.0) {Cross-Device};
  \node[font=\bfseries, anchor=east] at (-0.2, 0.6) {Cross-Silo};

  \node[hl]   at (1.6, 2.0) {CultureQuest};
  \node[cell] at (5.0, 2.0) {};
  \node[cell] at (8.4, 2.0) {};

  \node[cell] at (1.6, 0.6) {};
  \node[cell] at (5.0, 0.6) {};
  \node[cell] at (8.4, 0.6) {};
\end{tikzpicture}
\caption{Poziționarea CultureQuest în taxonomia FL, pe axa tipului de FL (HFL/VFL/FTL) și axa modului de desfășurare (Cross-Device/Cross-Silo). Adaptat din Yang et al. 2019 \cite{yang2019fml} și Kairouz et al. 2021 \cite{kairouz2021advances}.}
\label{fig:taxonomie-fl}
\end{figure}

\section{Alternative respinse}

FedAvg \cite{mcmahan2017fedavg} este algoritmul de referință al FL. La fiecare rundă, un grup de K clienți antrenează local pe propriile date, iar serverul agregă actualizările prin medie ponderată. Eterogenitatea datelor dintre clienți este una dintre provocările cele mai notabile ale FL \cite{li2020challenges}, iar o direcție importantă de cercetare propune algoritmi care corectează deriva (\textit{drift}) dintre actualizările locale ale clienților și modelul global. FedProx \cite{li2020fedprox} adaugă funcției de cost locale un termen proximal, care penalizează distanța dintre ponderile actualizate și cele ale modelului global de la începutul rundei. SCAFFOLD \cite{karimireddy2020scaffold} introduce, pentru fiecare client, o variabilă de control care estimează cât de mult diferă actualizarea sa de media cohortei și corectează gradientul local pe baza acestei estimări. FedDyn \cite{acar2021feddyn} ajunge la un rezultat similar printr-un regularizator dinamic, recalculat la fiecare rundă pe baza istoricului actualizărilor clientului. Toate cele trei leagă actualizarea unui client de comportamentul celorlalți clienți selectați în aceeași rundă.

Această legătură nu are, însă, un corespondent în CultureQuest, unde agregarea se realizează prin FedAsync \cite{xie2019fedasync}, care procesează, câte o actualizare pe rând, pe măsură ce sosește de la un client, fără să aștepte o cohortă. Antrenarea locală urmează, totuși, logica FedAvg (SGD pe interacțiunile locale ale utilizatorului). Fără cohorte, nu există actualizări paralele de reconciliat la final de rundă, dar nici conceptul de medie a grupului față de care un client să se abată - condiții pe care se bazează atât termenul proximal din FedProx, cât și variabilele de control din SCAFFOLD sau regularizatorul din FedDyn. Aplicat în acest context, un astfel de termen de corecție ar trage actualizarea locală către un punct de referință arbitrar, fără ca vreo garanție de convergență din literatură să mai fie valabilă, întrucât toate cele trei presupun, în demonstrațiile lor, runde sincrone cu mai mulți clienți. De aceea, design-ul asincron al CultureQuest are nevoie de altceva - un mecanism care reduce influența actualizărilor venite de la clienți cu un model local prea vechi. \textbf{Tabelul~\ref{tab:fl-alternative}} rezumă această comparație.

\begin{table}[h]
\centering
\footnotesize
\caption{Comparație între algoritmii FL principali și soluția adoptată de CultureQuest.}
\label{tab:fl-alternative}
\begin{tabular}{|l|c|p{5.5cm}|c|}
\hline
\textbf{Algoritm} & \textbf{Sincron?} & \textbf{Mecanism} & \textbf{1 client/rundă?} \\
\hline
FedAvg \cite{mcmahan2017fedavg} & Da & Medie ponderată a cohortei de K clienți & Nu \\
\hline
FedAsync \cite{xie2019fedasync} & Nu & \textit{Discount staleness}: $1/(1+s)$ & Da \\
\hline
FedProx \cite{li2020fedprox} & Da & Termen proximal (penalizare față de modelul global) & Nu \\
\hline
SCAFFOLD \cite{karimireddy2020scaffold} & Da & Variabilă de control per client & Nu \\
\hline
FedDyn \cite{acar2021feddyn} & Da & Regularizator dinamic per client & Nu \\
\hline
CultureQuest & Nu & FedAvg (doar pentru antrenarea locală) + FedAsync & Da \\
\hline
\end{tabular}
\end{table}

\section{Tehnologiile utilizate și justificarea acestora}

Alegerea tehnologiilor pentru CultureQuest a fost ghidată de câteva criterii comune, precum compatibilitatea \textit{cross-platform} pentru aplicația mobilă, ecosistemul care se integrează natural cu componenta FL, cu inferența modelului și cu soluții \textit{open-source} care pot fi găzduite local. În secțiunile următoare se descriu aceste alegeri, componentă cu componentă.

\subsection{Flutter și Riverpod}

Pentru aplicația mobilă, CultureQuest folosește Flutter\footnote{Documentația oficială Flutter. \url{https://flutter.dev} (accesat 19.06.2026).} și Riverpod\footnote{Documentația oficială Riverpod. \url{https://riverpod.dev} (accesat 19.06.2026).} pentru gestionarea stării. Flutter are avantajul de a permite o singură bază de cod Dart pentru Android și iOS, ceea ce înjumătățește efortul de mentenanță față de dezvoltarea nativă pe cele două platforme, fiind relevant, mai ales, pentru a nu duplica logica clientului FL și, totodată, pentru a nu îngreuna sincronizarea. Flutter compilează codul Dart direct în cod nativ ARM, fără un \textit{bridge} către componentele native, eliminându-se \textit{overhead-ul} de comunicare regăsit în React Native la fiecare apel nativ\footnote{Flutter, documentație privind performanța și arhitectura de compilare. \url{https://docs.flutter.dev/perf} (accesat 19.06.2026).}. În ceea ce privește Riverpod, acesta oferă un model de gestionare a stării declarativ și testabil. Este, prin urmare, potrivit pentru o aplicație în care starea ecranului curent trebuie sincronizată cu procese care rulează în fundal, precum antrenarea locală a modelului.

\subsection{FastAPI, MongoDB și Redis}

Pe partea de \textit{backend}, CultureQuest folosește FastAPI\footnote{Documentația oficială FastAPI. \url{https://fastapi.tiangolo.com} (accesat 19.06.2026).}, MongoDB\footnote{Documentația oficială MongoDB. \url{https://www.mongodb.com/docs/} (accesat 19.06.2026).} și Redis\footnote{Documentația oficială Redis. \url{https://redis.io/docs/} (accesat 19.06.2026).}. FastAPI este scris în Python, ceea ce permite integrarea directă cu ecosistemul NumPy folosit la pre-antrenare și la inferența MLP, fără strat de conversie între limbaje. Mai mult, suportul nativ pentru operații asincrone\footnote{FastAPI, documentație privind operațiile asincrone. \url{https://fastapi.tiangolo.com/async/} (accesat 19.06.2026).} se potrivește cu un număr mare de clienți conectați intermitent. Alegerea MongoDB, adică a unei baze de date non-relaționale, se justifică prin stocarea de date eterogene cu câmpuri diferite - obiective, \textit{quest-uri}, povești, recenzii - care într-o bază de date relațională precum PostgreSQL ar necesita tabele de \textit{join} suplimentare. Interogările de proximitate sunt acoperite de indexul 2dsphere nativ al MongoDB și, deci, nu este nevoie de o extensie precum PostGIS, ale cărei capabilități GIS avansate nu sunt necesare când OSRM se ocupă deja de partea de rutare. Redis, la rândul lui, acoperă starea nepersistentă a procesului FL, cu o latență de sub 1 ms pentru operații în memorie și cu suport pentru \texttt{SET NX PX} atomic - pentru lock-ul de agregare (\texttt{fl:agg\_lock}) care protejează ciclul read-aggregate-write.

\subsection{Motorul de rutare OSRM}

Funcționalitatea de rutare se bazează pe OSRM (\textit{Open Source Routing Machine})\footnote{Documentația oficială OSRM. \url{https://project-osrm.org} (accesat 19.06.2026).}, un motor \textit{open-source} bazat pe date OpenStreetMap. Poate fi găzduit local, pe același server cu restul serviciilor de \textit{backend}, fără dependență de un API extern plătit (Google Maps Direction API facturează, de exemplu, per cerere\footnote{Google Maps Platform, prețuri Directions API. \url{https://developers.google.com/maps/documentation/directions/usage-and-billing} (accesat 19.06.2026).}). OSRM calculează rute pe rețeaua de drumuri folosind Contraction Hierarchies, și astfel, răspunde în câteva zeci de milisecunde pentru o rută obișnuită ca distanță în perimetrul unui oraș\footnote{Luxen, D., \& Vetter, C. (2011). Real-time routing with OpenStreetMap data. \textit{Proceedings of the 19th ACM SIGSPATIAL}. \url{https://doi.org/10.1145/2093973.2094062}} - suficient de rapid pentru a fi folosit de utilizator pe parcursul unei rute.

\subsection{Perceptron multi-strat}

Modelul de recomandare al CultureQuest este un MLP. Arhitectura este simplă: trei straturi complet conectate (22-32-16-1), pornind de la un vector de 22 de caracteristici asociate obiectivelor turistice și culturale. Pe dispozitiv, modelul nu este folosit doar pentru antrenarea locală, ci și pentru afișare - de fiecare dată când utilizatorul deschide pagina cu informații a unui obiectiv turistic, aplicația face o nouă trecere \textit{forward pass} prin model, ca să calculeze scorul de potrivire afișat pentru turist. Tocmai pentru că rulează la fiecare interacțiune, modelul trebuie să fie o implementare proprie, cât mai ușoară și fără dependențe externe.

Alternativele cunoscute pentru rularea unui model de rețea neuronală pe un dispozitiv sunt TFLite\footnote{Documentația oficială TensorFlow Lite. \url{https://www.tensorflow.org/lite} (accesat 19.06.2026).} și PyTorch Mobile\footnote{PyTorch Mobile este parțial înlocuit de ExecuTorch; documentația oficială ExecuTorch. \url{https://docs.pytorch.org/executorch/stable/index.html} (accesat 19.06.2026).}, ambele \textit{runtime-uri} dedicate, cu dependențe native pentru fiecare platformă. TFLite adaugă $\approx$1,3 MB\footnote{Google. TFLite: reduce binary size. \url{https://www.tensorflow.org/lite/guide/reduce_binary_size} (accesat 19.06.2026).} la APK (arm64), PyTorch Mobile $\approx$30 MB\footnote{PyTorch. PyTorch Mobile. \url{https://pytorch.org/mobile/home/} (accesat 19.06.2026).}, fără avantaje vizibile pentru un model de 5 KB. Implementarea în Dart rămâne, deci, mai mică, fără dependențe de \textit{runtime} suplimentare, și compatibilă nativ cu Flutter, după cum reiese și din \textbf{tabelul~\ref{tab:mlp-comparativ}}.

\clearpage
\begin{table}[h]
\centering
\footnotesize
\caption{Comparație între implementarea MLP proprie și runtime-urile dedicate (TFLite, PyTorch Mobile) pentru inferență pe dispozitiv.}
\label{tab:mlp-comparativ}
\begin{tabular}{|l|p{3.2cm}|p{2.8cm}|p{2.5cm}|p{2cm}|}
\hline
\textbf{Implementare} & \textbf{Parametri / Dimensiune model} & \textbf{Runtime adăugat la APK} & \textbf{Dependențe native} & \textbf{Timp inferență} \\
\hline
MLP propriu (Dart) & 1.281 param. (22-32-16-1), $\approx$5 KB (valori în format float32) & 0 MB (compilat nativ) & Niciuna & $\approx$40 µs \\
\hline
TFLite & depinde de model & $\approx$1,3 MB (arm64) & Bibliotecă pentru Android/iOS & <1 ms \\
\hline
PyTorch Mobile & depinde de model & $\approx$30 MB (arm64) & Bibliotecă pentru Android/iOS & <1 ms \\
\hline
\end{tabular}
\end{table}

\chapter{Soluția Propusă}

Acest capitol detaliază proiectarea CultureQuest, cu suportul a câtorva diagrame, tabele și ecuații care justifică deciziile de design. Pornește de la o privire de ansamblu asupra arhitecturii, după care urmează aplicația mobilă și serviciile \textit{backend}, fiecare cu propria diagramă de structură. Cea mai mare parte a capitolului este dedicată proiectării sistemului FL (4.4). Urmează fluxul de generare a rutelor, elementele de gamificare și harta cu navigarea, iar în final capitolul se încheie cu arhitectura de confidențialitate, care descrie deciziile de proiectare legate de separarea datelor între dispozitiv și server.

\section{Prezentare generală a sistemului}

La nivel general, CultureQuest este format din patru componente: aplicația mobilă, scrisă în Flutter, un \textit{backend} FastAPI, o bază de date MongoDB pentru datele persistente și o instanță Redis pentru starea nepersistentă a procesului FL. \textbf{Figura~\ref{fig:arhitectura-generala}} arată comunicarea dintre ele. Aplicația mobilă este singurul punct de legătură dintre utilizator și sistem - toate cererile trec prin \textit{backend}, care, în funcție de tipul de date, citește sau scrie în MongoDB sau în Redis.

Diagrama evidențiază trei fluxuri principale, pe care le-am numerotat cu (1), (2) și (3). Primul (1), cererea de hartă și rute, funcționează astfel: aplicația mobilă trimite poziția curentă, \textit{backend-ul} interoghează baza de date pentru obiectivele din apropiere, le oferă un scor prin intermediul modelului FL global ținut în Redis și calculează, pe baza scorurilor, o rută pe care o trimite înapoi. Al doilea (2), agregarea FL, funcționează similar: dispozitivul trimite actualizarea modelului antrenat local, \textit{backend-ul} o agregă cu ajutorul stării ținute în Redis și trimite înapoi modelul global actualizat. Recenziile, poveștile și \textit{quest-urile} adăugate de utilizatori formează al treilea flux (3), unde acestea trec printr-un pas de moderare înainte să fie salvate în MongoDB.

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{pics/arhitectura-generala.pdf}
\caption{Arhitectura generală a sistemului CultureQuest: aplicația mobilă comunică exclusiv cu \textit{backend-ul}, care citește sau scrie în MongoDB și Redis.}
\label{fig:arhitectura-generala}
\end{figure}

\section{Arhitectura aplicației mobile}

Aplicația mobilă este organizată pe module de tip \textit{feature}, unde fiecare funcționalitate principală este asociată unui director propriu cu ecrane și provideri, fără dependențe directe față de alte module. Cele șapte module sunt \textit{map} - pentru hartă și obiectivele de pe aceasta, dar și pentru generare de rute, \textit{auth} - pentru autentificare, \textit{federated} - pentru clientul FL, \textit{onboarding} - pentru alegerea intereselor de către utilizator la instalarea aplicației, \textit{profile} - pentru profilul utilizatorului și statusul procesului FL, \textit{proximity} - pentru partea \textit{geofencing} și detectarea proximității față de obiective și \textit{quest} - pentru sistemul de misiuni.

Gestionarea stării intră în responsabilitatea Riverpod, care leagă modulele între ele prin provideri declarativi. Aceștia sunt disponibili de la orice nivel al arborelui de \textit{widget-uri}, rezultând avantajul că datele nu mai sunt transmise manual prin constructori. \textbf{Figura~\ref{fig:arhitectura-mobila}} ilustrează arhitectura în trei straturi. Modulele de tip \textit{feature} folosesc provideri Riverpod specializați, care la rândul lor delegă atât cererile către \textit{backend}, cât și operațiile de stocare locală altor servicii dedicate. Providerii partajați arată dependențele funcționale dintre module: \textit{fl\_provider} este utilizat de \textit{map} (pentru scorul de potrivire afișat în cadrul unui obiectiv), de \textit{federated} (pentru statusul agregării FL) și de \textit{profile} (pentru statusul antrenării locale). De asemenea, \textit{proximity\_provider} este partajat de \textit{proximity} și \textit{quest}, cel din urmă depinzând de evenimentele de \textit{geofencing} pentru a permite completarea \textit{quest-urilor} disponibile pentru un obiectiv.

\begin{figure}[h]
\centering
\hspace*{-0.4cm}\includegraphics[width=1.05\textwidth]{pics/arhitectura-mobila.pdf}
\caption{Arhitectura aplicației mobile CultureQuest: modulele de tip \textit{feature} folosesc \textit{providere} Riverpod partajate.}
\label{fig:arhitectura-mobila}
\end{figure}

\section{Arhitectura serviciilor \textit{backend}}

\textit{Backend-ul} aplicației este implementat cu FastAPI și structurat pe domenii de funcționalitate. Fiecare colecție MongoDB are un grup de rute dedicat și un serviciu de logică de business asociat. Grupurile de rute sunt împărțite în felul următor: un grup pentru autentificarea utilizatorilor (\textit{auth}, cu înregistrare și autentificare pe bază de token JWT), obiectivele turistice și culturale (\textit{landmarks}), misiunile (\textit{quests}), rutele turistice (\textit{routes}), evenimentele (\textit{events}), recenziile (\textit{comments}), validarea comunitară a conținutului (\textit{community}) și schimbul de ponderi FL cu clienții (\textit{federated}).

Modelul de date - a cărui schemă completă este ilustrată în \textbf{figura~\ref{fig:db}} din Anexe - este construit în jurul colecțiilor \textbf{USERS} și \textbf{LANDMARKS}: USERS stochează profilul utilizatorului, format din credențialele acestuia și lista de interese pe care le poate selecta, și care intră ulterior în construirea vectorului de caracteristici pentru modelul FL, iar LANDMARKS reprezintă obiectivele care au drept caracteristică principală locația în format GeoJSON indexată 2dsphere, necesară interogărilor de proximitate. De colecția LANDMARKS sunt legate direct \textbf{QUESTS}, \textbf{EVENTS}, \textbf{COMMENTS}, \textbf{STORIES} și \textbf{VISITS}, dar și \textbf{ROUTES} pentru stocharea rutelor ca secvențe ordonate de referințe către LANDMARKS (fie rute globale pre-generate, fie generate de utilizatorii care doresc să parcurgă un traseu cu obiectivele turistice din apropiere).

Conținutul generat de utilizatori trece printr-un flux de moderare înainte de publicare. \textit{Quest-urile} propuse și poveștile sunt create cu statusul \textit{pending} și devin disponibile altor utilizatori doar după aprobarea de către un administrator sau de către cinci alți utilizatori. Comentariile sunt, însă, publicate imediat, dar pot fi marcate automat prin câmpul \textit{flagged} de modulul de moderare a conținutului, acestea ajungând să fie vizibile conturilor de tip administrator pentru a fi revizuite manual.

\section{Proiectarea sistemului de învățare federată}

Sistemul FL din CultureQuest este proiectat să asocieze un scor de potrivire fiecărui obiectiv și să personalizeze generarea rutelor fără ca datele de interacțiune să părăsească dispozitivul utilizatorului. Fluxul FL este structurat în șase componente: un model MLP de dimensiuni reduse care rulează pe dispozitiv, o procedură de antrenare locală bazată pe FedAvg, un mecanism de agregare asincronă cu reducere pe bază de vechime conform FedAsync, limitarea normei actualizărilor agregate pentru ca modelul să nu fie afectat de contribuțiile extreme, adăugarea de zgomot Gaussian pentru garantarea DP, dar și un mecanism de control al concurenței bazat pe Redis.

\subsection{Arhitectura modelului}

Modelul folosit pentru personalizarea din cadrul aplicației este un MLP cu arhitectura $22 \to 32 \to 16 \to 1$, model ilustrat în \textbf{figura~\ref{fig:mlp}}, în care se descrie faptul că straturile ascunse folosesc funcția de activare ReLU, pe când stratul de ieșire aplică funcția sigmoid, producând un scor continuu în $[0, 1]$ care determină gradul de potrivire dintre profilul utilizatorului și un obiectiv dat. Modelul însumează 1.281 de parametri, acest număr mic de parametri fiind ales deliberat pentru a menține costul computațional redus pe dispozitiv. Acesta rulează atât în antrenare, cât și la fiecare deschidere a fișei unui obiectiv.

Vectorul de intrare de 22 de dimensiuni grupează caracteristicile în patru categorii, iar fiecare caracteristică este detaliată în \textbf{tabelul~\ref{tab:features}}. Primele 14 dimensiuni codifică profilul de interese al utilizatorului și tipul obiectivului în format \textit{one-hot}. Contextul temporal (dimensiunile 14-16) stabilește disponibilitatea obiectivului la momentul interogării, dacă la momentul actual este weekend și mai include ora normalizată la $[0, 1]$. Contextul spațial și de rută (dimensiunile 17-21) includ rangul relativ de distanță față de alte posibile obiective care pot fi alese pentru rută, scorul de potrivire a intereselor și trei caracteristici care sunt active doar în etapa de generare a rutelor.

Același model rulează pentru fiecare locație în parte, cu un vector de caracteristici construit specific acesteia, scorul rezultat fiind convertit într-o categorie de potrivire afișată pe fișa obiectivului - \textbf{scăzut} pentru scoruri sub 0,4, \textbf{mediu} pentru intervalul $[0{,}4,\ 0{,}7)$ și \textbf{ridicat} pentru scoruri mai mari sau egale decât 0,7.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{pics/mlp.pdf}
\caption{Arhitectura modelului MLP utilizat pentru personalizare.}
\label{fig:mlp}
\end{figure}

\begin{table}[h]
\centering
\footnotesize
\caption{Vectorul de caracteristici de intrare al modelului FL (22 dimensiuni).}
\label{tab:features}
\begin{tabular}{|c|p{3.5cm}|p{2cm}|p{6cm}|}
\hline
\textbf{Dim.} & \textbf{Caracteristică} & \textbf{Tip} & \textbf{Descriere} \\
\hline
0--5  & Interesele utilizatorului  & one-hot       & artă, arhitectură, istorie, gastronomie, natură, muzică \\
\hline
6--13 & Tipul obiectivului         & one-hot       & muzeu, monument, parc, galerie, restaurant, piață, clădire, altele \\
\hline
14    & isOpen                     & binar         & obiectivul este deschis \\
\hline
15    & isWeekend                  & binar         & ziua curentă este sâmbătă sau duminică \\
\hline
16    & oră normalizată            & float $[0,1]$ & ora curentă împărțită la 24 \\
\hline
17    & relativeDistanceRank       & float $[0,1]$ & proporția candidaților mai apropiați; 0 = cel mai apropiat \\
\hline
18    & interestMatchScore         & float $[0,1]$ & proporția categoriilor comune între utilizator și obiectiv \\
\hline
19    & isPartOfRoute              & binar         & obiectivul face parte din ruta curentă \\
\hline
20    & routeStopNormalized        & float $[0,1]$ & poziția opririi în rută; 0 = prima oprire, 1 = ultima \\
\hline
21    & routeLengthNormalized      & float $[0,1]$ & lungimea rutei normalizată la maxim 5 opriri \\
\hline
\end{tabular}
\end{table}

\subsection{Antrenarea locală}

Procedura de antrenare locală este implementată conform protocolului clientului din FedAvg \cite{mcmahan2017fedavg}. Dispozitivul antrenează modelul pe un buffer local de interacțiuni și trimite ulterior către server doar variația ponderilor față de modelul global. O rundă de antrenare se declanșează automat după strângerea a 15 interacțiuni în buffer, pragul ales asigurând un set minim divers de exemple înainte de actualizare. Runda poate fi inițiată și manual, de către utilizator, dacă au fost strânse cel puțin 8 interacțiuni cu obiectivele, pragul ales fiind mai mic aici, deoarece utilizatorul alege conștient să trimită actualizările către server. Fiecare rundă presupune rularea a 5 epoci complete peste întregul set local, număr testat în literatura de bază FL \cite{mcmahan2017fedavg}, aducând un echilibru între reducerea numărului de runde de învățare și limitarea derivei locale a modelului față de cel global.

Optimizarea se realizează prin SGD cu rata de învățare $\eta = 0{,}01$, care este aplicată pentru fiecare eșantion, valoarea conservatoare prevenind divergența în prezența zgomotului Gaussian adăugat în pasul de DP. Pentru funcția de pierdere se folosește eroarea medie pătratică (MSE, \textit{Mean Squared Error}), cu formula actualizărilor ilustrată în \textbf{ecuația~\ref{eq:sgd}}. Alegerea MSE față de BCE este motivată de natura etichetelor de antrenare: acestea sunt valori continue în $[0, 1]$, nu valori de 0 sau 1 discrete, iar gradienții MSE sunt mărginiți, pe când BCE are gradienți nemărginiți pe măsură ce predicția se apropie de 0 sau 1, ceea ce poate destabiliza antrenarea în prezența zgomotului Gaussian adăugat în pasul de DP; însă, simularea evaluează convergența cu BCE, care este o metrică mai sensibilă pentru modelul cu ieșire sigmoid, deoarece scara logaritmică penalizează mai sever predicțiile confident greșite față de eroarea pătratică. Gradientul acestor actualizări este limitat la norma L2 unitară, iar dacă norma depășește 1, gradientul este scalat proporțional. Procesul este diferit de limitarea variației ponderilor aplicată ulterior în pasul de DP.

\begin{equation}
w_{t+1} = w_t - \eta \cdot \frac{g_t}{\max\!\left(1,\, \|g_t\|_2\right)}
\label{eq:sgd}
\end{equation}

\noindent unde $w_t$ reprezintă ponderile la pasul $t$, $\eta = 0{,}01$ este rata de învățare, $g_t$ este gradientul erorii medii pătratice calculat per eșantion, iar $\|g_t\|_2$ este norma sa L2.

\subsection{Agregarea asincronă și reducerea în funcție de vechime}

Serverul agregă imediat fiecare actualizare primită, fără a aștepta alți clienți, indiferent dacă este vorba doar de un subset dintre ei, procesul de învățare de pe dispozitiv și apoi agregarea reprezentând o rundă. Clientul trimite către server numărul rundei la care a descărcat modelul global (\texttt{client\_round}), iar \textit{staleness-ul} reprezintă câte runde au trecut de când clientul a primit modelul actualizat. Factorul de reducere \cite{xie2019fedasync}, prezentat în \textbf{ecuația~\ref{eq:fedasync}}, scalează numărul efectiv de eșantioane.

Modelul global este ponderat cu de 4 ori mai multe eșantioane virtuale, astfel încât contribuția unui client să nu poată depăși 20\% din agregare. Această limitare compensează absența rotunjirii sincrone: în FedAvg clasic, diluarea contribuțiilor individuale apare natural din numărul de clienți participanți simultan, iar în varianta asincronă, unde clienții sunt procesați pe rând, același efect trebuie impus explicit. Am ales și un prag minim de 20 de eșantioane virtuale care asigură că, pentru clienții cu discount de \textit{staleness} extrem, procentul de contribuție este cel mult $\approx$5\%.

Funcția de reducere este ilustrată în \textbf{figura~\ref{fig:fedasync}}, unde se observă că la \textit{staleness}=0 clientul contribuie integral, la \textit{staleness}=1 în proporție de 50\%, iar la \textit{staleness}=9 în proporție de doar 10\%. Alegerea algoritmului se justifică prin următorul caz: dacă doi - sau mai mulți - clienți descarcă modelul global în același timp și unul termină antrenarea mai repede, serverul ajunge la runda următoare înainte ca al doilea să trimită actualizarea. Prin urmare, discountul penalizează proporțional contribuția celui rămas în urmă cu varianta modelului global.

\begin{equation}
\mathrm{discount}(s) = \frac{1}{1 + s}, \quad n_{\mathrm{eff}} = \max\!\left(1,\ \left\lfloor n \cdot \mathrm{discount}(s) \right\rfloor\right)
\label{eq:fedasync}
\end{equation}

\noindent unde $s = \texttt{current\_round} - \texttt{client\_round}$ este vechimea actualizării, $n$ este numărul de eșantioane locale ale clientului, iar $n_{\mathrm{eff}}$ este numărul efectiv folosit la agregare.

\begin{figure}[h]
\centering
\includegraphics[width=0.65\textwidth]{pics/fedasync.pdf}
\caption{Factorul de reducere FedAsync în funcție de \textit{staleness}.}
\label{fig:fedasync}
\end{figure}

\subsection{Limitarea gradienților}

Înainte de agregare, serverul calculează variația ponderilor față de modelul global, denumită și delta. Dacă norma L2 a deltei depășește pragul $\tau = 1$, aceasta este rescalată proporțional conform \textbf{ecuației~\ref{eq:clipping}} \cite{mcmahan2018dpfedavg}, după care actualizarea rescalată este trecută prin agregarea ponderată FedAvg. Pentru un client care antrenează în mod normal, delta are o normă mică și factorul de scalare este 1, deci, actualizarea trece nemodificată. Limitarea intervine doar în cazul în care delta este foarte mare față de ce ar produce o antrenare obișnuită.

Rolul principal al acestui tip de limitare este de a reduce influența actualizărilor extreme. Un client malițios care evaluează totul adversarial \cite{lyu2020threats} produce o deltă cu normă mare, dar aceasta este redusă la $\tau$ înainte de agregare, limitând influența sa pentru fiecare coordonată de pondere, însă, limitarea deltei este și o condiție necesară pentru garantarea DP din secțiunea următoare.

\begin{equation}
\tilde{\delta} = \delta \cdot \min\!\left(1,\ \frac{\tau}{\|\delta\|_2}\right)
\label{eq:clipping}
\end{equation}

\noindent unde $\delta = w_{\mathrm{client}} - w_{\mathrm{global}}$ este variația ponderilor față de modelul global, $\|\delta\|_2$ este norma sa L2, iar $\tau = 1$ este pragul de limitare.

\subsection{Confidențialitate diferențială}

Perturbarea prin zgomot nu se face central, zgomotul Gaussian fiind adăugat pe dispozitivul clientului, nu pe server, conform mecanismului NbAFL (\textit{Noising before Model Aggregation Federated Learning}) \cite{wei2020nbafl}. Imediat după etapa de antrenare locală, fiecare coordonată a deltei este limitată la intervalul $[-\tau, \tau]$, și abia peste valorile rezultate se suprapune zgomotul. În felul acesta, la server ajung ponderile cu zgomotul deja aplicat, din care actualizarea reală nu mai poate fi reconstituită niciodată.

Limitarea făcută coordonată cu coordonată la acest pas nu trebuie confundată cu limitarea normei L2 discutată la secțiunea anterioară: aceea tratează delta ca pe un singur vector și are rolul de a apăra modelul de actualizările adversariale \cite{lyu2020threats}, pe când cea de față ține sensibilitatea mărginită, condiție care face ca zgomotul adăugat să aibă efect real asupra confidențialității \cite{shokri2017membership}. Cât despre intensitatea lui, valoarea aleasă (0,1) reprezintă un echilibru asumat între confidențialitate și utilitatea modelului, pe care îl analizez în secțiunea de evaluare (Capitolul 6).

\begin{equation}
\tilde{w} = w_{\mathrm{global}} + \mathrm{clip}(\delta,\ \tau) + \mathcal{N}\!\left(0,\ (\sigma \cdot \tau)^2\right)
\label{eq:dp}
\end{equation}

\noindent unde $\delta = w_{\mathrm{trained}} - w_{\mathrm{global}}$ este variația ponderilor, $\tau = 1$ este pragul de limitare, iar $\sigma = 0{,}1$ este multiplicatorul de zgomot.

\subsection{Controlul concurenței}

Citirea modelului global, agregarea și rescrierea lui nu alcătuiesc o operație atomică în mod implicit. Dacă doi clienți trimit actualizări în același timp, se poate ajunge ca ambii să citească modelul la aceeași versiune, să-și calculeze delta fiecare, independent, dar să-și suprascrie reciproc rezultatul agregării. Pentru a preveni acest caz, am implementat pentru server un \textit{lock} Redis bazat pe \texttt{SET NX PX}: un token unic care este scris în Redis sub cheia \texttt{fl:agg\_lock}, doar dacă aceasta nu există deja, ceea ce asigură că un singur client poate intra în etapa de agregare la un moment dat. Eliberarea \textit{lock-ului} se face printr-un script Lua rulat atomic, care mai întâi verifică că token-ul aparține clientului curent pentru a evita ștergerea eronată a unui \textit{lock} deținut de alt client.

Dacă \textit{lock-ul} nu poate fi obținut, serverul mai încearcă de 9 ori, la intervale de câte 0,3 secunde - fereastra totală de 2,7 secunde acoperă scenariul a 10 cereri simultane, unde latența p95 a agregării este de 2,44 secunde (\textit{load test}, Capitolul 6) - iar dacă tot nu reușește, întoarce o eroare. \textit{Lock-ul} expiră de la sine după 5 secunde (valoare aleasă să depășească p95 la 10 cereri), în cazul în care serverul devine indisponibil în mijlocul agregării și \textit{lock-ul} rămâne blocat. Am redat fluxul întreg în \textbf{figura~\ref{fig:redis-lock}}.

\begin{figure}[h]
\centering
\includegraphics[width=0.75\textwidth]{pics/redis-lock.pdf}
\caption{Controlul concurenței prin lock Redis.}
\label{fig:redis-lock}
\end{figure}

\section{Fluxul de generare a rutelor}

Generarea unei rute cuprinde, mai întâi, locația curentă a utilizatorului, rezultând, în final, un traseu ordonat de obiective din proximitate, alcătuit astfel încât să respecte preferințele legate de interese și un buget alocat de maxim 300 de minute (o sesiune tipică de jumătate de zi). Calculul rulează pe server, în \texttt{route\_service.py}, și este schițat în \textbf{figura~\ref{fig:route-flow}}. În prima etapă se strâng toate obiectivele turistice aflate pe o rază de 10 km, fiind o distanță care acoperă perimetrul centrului unui oraș și corespunde aproximativ cu două ore de deplasare pe jos. Apoi, li se atribuie un scor inițial care combină scorul FL (în proporție de 80\%) și proximitatea față de locația de start (în proporție de 20\%). Ponderea mare a FL se justifică prin faptul că vectorul de caracteristici include deja rangul relativ de distanță, aceasta fiind deja luată în considerare indirect, pe când componenta de proximitate de 20\% previne situația în care un obiectiv îndepărtat cu scor mare din punct de vedere FL este ales primul. Dacă modelul global nu este disponibil în momentul respectiv, locul scorului FL este preluat de o fracție de potrivire a intereselor, fiind dedusă direct din categoriile obiectivului. Primele 10 locații - dacă există în apropiere - cu cel mai mare scor rămân drept candidați.

Am ales ca traseul să fie construit cu ajutorul unui algoritm greedy de tip vecin cel mai apropiat, adecvat pentru numărul mic de opriri dintr-o rută turistică (cel mult 5), deoarece o abordare exactă ar avea un cost computațional mai mare fără beneficii vizibile. La fiecare pas, se alege obiectivul cel mai aproape de poziția curentă, însă, cu o corecție care „scurtează" distanța față de candidații ale căror categorii se potrivesc intereselor utilizatorului. Pentru fiecare oprire se estimează și o durată de vizitare, durata care pleacă de la un timp de bază specific tipului de obiectiv, estimat pe baza comportamentului tipic al turiștilor sau localnicilor (de la 15 minute pentru o piață publică până la 70 de minute pentru un muzeu). Mai mult, este scalată între 75\% și 125\% în funcție de scorul FL al opririi. Intervalul de $\pm$25\% este suficient de mare pentru a reflecta preferința utilizatorului, dar nu atât de extrem încât să distorsioneze în mare măsură bugetul total de timp al rutei.

\begin{figure}[h]
\centering
\hspace*{1.5cm}
\includegraphics[width=1\linewidth]{pics/route-flow.pdf}
\caption{Fluxul de generare a rutelor.}
\label{fig:route-flow}
\end{figure}

\section{Proiectarea elementelor de gamificare}

Gamificarea se sprijină pe sistemul de \textit{quest-uri} asociate obiectivelor turistice și culturale - sarcini scurte pe care utilizatorul le poate accepta și duce la capăt doar la fața locului, fiind răsplătit cu puncte la completare. Un \textit{quest} poate fi adăugat atât de un administrator sau de o persoană care a creat obiectivul asociat pe hartă (și va fi disponibil imediat altor utilizatori), cât și de o persoană oarecare (în acest caz fiind nevoie ori de aprobarea unui administrator, ori de votul a cinci utilizatori fără rol de administrator). Pragul de cinci voturi este suficient de mic pentru a fi atins într-o comunitate activă, și, în același timp, suficient de mare pentru a aduce mai mulți utilizatori independenți laolaltă, iar voturile sunt anonime, păstrându-se principiul de confidențialitate al aplicației.

Fiecare interacțiune cu un obiectiv lasă în urmă un scor de \textit{engagement} al turistului cu obiectivul, care reprezintă ținta de antrenare a modelului FL pentru acea interacțiune. Valoarea scorului crește odată cu „intensitatea interacțiunii": deschiderea fișei unui obiectiv generează valoarea 0,3, adăugarea la rută a obiectivului sau un răspuns greșit la un \textit{quest} aduce un scor de 0,5, iar un răspuns corect 0,9. Am ales valorile empiric, pentru a reprezenta o bună corelație cu dorința de implicare în a descoperi mai mult despre obiectivul curent. Așadar, scorul maxim generat prin aceste interacțiuni va deveni scorul asociat obiectivului, și va fi folosit pentru etapa de \textit{backpropagation}. Evaluările cu stele sunt convertite după formula $n/5$, producând valori între 0,2 și 1 în funcție de numărul de stele lăsat de utilizator în cadrul recenziei, dar, în acest caz, valoarea rezultată suprascrie orice valoare produsă de interacțiunile anterioare, chiar dacă ar fi mai mare. Setul complet de etichete este redat în \textbf{tabelul~\ref{tab:engagement}}.

\begin{table}[h]
\centering
\footnotesize
\caption{Etichete de \textit{engagement}.}
\label{tab:engagement}
\begin{tabular}{|l|l|}
\hline
\textbf{Eveniment} & \textbf{Etichetă} \\
\hline
Fișa obiectivului deschisă & 0,3 \\
\hline
Obiectiv adăugat la rută & 0,5 \\
\hline
\textit{Quest}: răspuns incorect & 0,5 \\
\hline
Navigare către obiectiv pornită & 0,6 \\
\hline
\textit{Quest} propus de utilizator & 0,8 \\
\hline
Poveste trimisă & 0,85 \\
\hline
\textit{Quest}: răspuns corect & 0,9 \\
\hline
Evaluare cu $n$ stele & $n/5$ \\
\hline
\end{tabular}
\end{table}

\section{Harta și navigarea}

Harta aplicației integrează tehnica de \textit{geofencing}, implementată manual pe baza fluxului de poziții GPS furnizat de pachetul \textit{geolocator}\footnote{Pachet Flutter pentru accesul la localizare GPS. \url{https://pub.dev/packages/geolocator} (accesat 19.06.2026)}. De fiecare dată când poziția se actualizează, serviciul de proximitate recalculează distanțele până la obiectivele din jur și declanșează un eveniment de intrare sau ieșire în momentul în care utilizatorul traversează raza unui obiectiv. Lista obiectivelor monitorizate nu este, însă, reîncărcată la nesfârșit, ci abia după ce utilizatorul s-a deplasat cu mai mult de 200 de metri. Orientarea hărții este gestionată, la rândul ei, prin două mecanisme: în repaus, pachetul \textit{flutter\_compass}\footnote{Pachet Flutter pentru citirea busolei magnetometrice. \url{https://pub.dev/packages/flutter_compass} (accesat 19.06.2026)} rotește harta după busola magnetometrică și ignoră schimbările sub $2^\circ$ pentru a nu prelua zgomotul senzorului; în modul de navigare activă, harta se rotește după direcția de deplasare raportată de GPS.

\section{Arhitectura de confidențialitate}

Arhitectura de confidențialitate a CultureQuest pornește de la ideea că datele trebuie separate explicit în funcție de locul de stocare. Tot ce ține de comportamentul concret al turistului - obiectivele vizitate, rating-urile date fiecărui obiectiv în parte, \textit{quest-urile} finalizate și rutele salvate - rămân local, pe telefon, prin \textit{SharedPreferences}, în directorul privat al aplicației, în așa fel încât alte aplicații să nu aibă acces la date, fiind o responsabilitate a \textit{sandbox-ului} Android/iOS, iar spre server vin doar informații agregate sau anonimizate. Concret, se incrementează un contor global de vizite pentru fiecare obiectiv în locul primirii identității vizitatorului, respectiv se modifică suma și numărul de rating-uri în locul evaluării de către vizitator. Pe server, totul se rezumă la a se păstra doar datele necesare funcționării aplicației: numele complet, punctajul total, numărul de \textit{quest-uri} completate și interesele selectate. Aceeași logică se aplică și componentei FL: serverul primește doar delta-ul ponderilor obținută după antrenarea local, peste care s-a adăugat deja zgomot Gaussian. Distribuția completă este redată în \textbf{tabelul~\ref{tab:privacy}}, iar modul în care sunt implementate mecanismele de confidențialitate este detaliat în Capitolul 5.

Arhitectura actuală are, însă, și o limitare: serverul primește delta-ul fiecărui client în mod individual, așa că se pot distinge, în principiu, contribuțiile individuale. Protocolul de agregare securizată~\cite{bonawitz2017secagg} ar elimina acest risc, făcând ca serverul să vadă doar suma actualizărilor, nu și delta-urile individuale. Numai că SecAgg este incompatibil cu configurația asincronă pe care se bazează platforma. Protocolul cere ca un set fix de clienți să fie activi în același timp pentru schimbul de chei criptografice, ceea ce contravine modelului FedAsync în care clienții se conectează independent, fiecare la momentul lui. Mai mult, discountul de \textit{staleness} aplicat pentru fiecare client la agregare are nevoie de acces la delta-urile individuale, nu doar la suma lor.

\clearpage
\begin{table}[h]
\centering
\footnotesize
\caption{Distribuția datelor în funcție de locul de stocare.}
\label{tab:privacy}
\begin{tabular}{|p{4cm}|p{4cm}|p{4.5cm}|}
\hline
\textbf{Date locale (SharedPreferences)} & \textbf{Date pe server} & \textbf{Ce nu se stochează pe server} \\
\hline
Lista obiectivelor vizitate & Numele utilizatorului  & Istoricul locației \\
\hline
Rating-uri individuale pentru fiecare obiectiv & Puncte totale acumulate & Care obiective au fost vizitate și când \\
\hline
\textit{Quest-uri} completate (care și cum) & Număr total de \textit{quest-uri} completate & Rating-urile individuale pentru fiecare obiectiv \\
\hline
Rute salvate & Interese selectate & Care \textit{quest-uri} au fost completate \\
\hline
Identitatea autorului recenziei & - & Autorul fiecărei recenzii sau povești \\
\hline
\end{tabular}
\end{table}

\chapter{Detalii de Implementare}

Dacă Capitolul 4 descrie ce face fiecare componentă și care au fost motivele din spate pentru fiecare, Capitolul 5 coboară cu un nivel mai jos și detaliază cum s-au implementat concret componentele, prezentându-se, mai întâi, partea de aplicație mobilă, cu serviciul client FL, mecanismul DP, bufferul de interacțiuni și restul componentelor care rulează pe dispozitiv. După aceea, urmează serviciile \textit{backend}, cu agregarea FL, punctele de acces API și moderarea conținutului. Urmează și două secțiuni care sunt în legătură cu partea de ML pentru recomandări, pentru că una descrie fluxul de pre-antrenare pe date publice, prin care modelul global este pregătit înainte de a contribui clienții, iar alta prezintă \textit{framework-ul} de simulare FL pe care se bazează evaluarea din Capitolul 6. De-a lungul capitolului, părți de cod atașate în document arată locurile unde deciziile de implementare sunt cele mai relevante, și anume: antrenarea local cu limitare a gradienților, adăugarea zgomotului Gaussian, agregarea FL cu limitare, moderarea printr-un API extern a conținutului și rularea unei runde de simulare. Se adună, în final, dificultățile întâlnite pe parcurs, făcând trecerea spre evaluarea rezultatelor.

\section{Implementarea aplicației mobile}

Implementarea mobilă acoperă șase subsecțiuni, pe care le-am descris în ordinea dependențelor: serviciul client FL, care implementează manual în Dart \textit{forward pass-ul}, partea de \textit{backpropagation} și limitarea gradienților, fără vreun \textit{framework} de ML. Legat de acest serviciu este mecanismul DP, care adaugă zgomot Gaussian variației de ponderi înainte de trimiterea de către client a actualizărilor, și bufferul local de interacțiuni, care păstrează scorurile de \textit{engagement} între runde și le stochează în \texttt{SharedPreferences}. Separat de FL, stocarea offline menține un \textit{cache} al obiectivelor turistice pe telefonul utilizatorilor, la care aplicația poate ajunge când \textit{backend}-ul nu este funcțional sau turistul nu are acces la o conexiune stabilă de rețea. Ultimele două subsecțiuni descriu componente orientate spre utilizator. Integrarea UI a generării rutelor înseamnă, odată, rularea algoritmului pe \textit{backend}, dar și afișarea rutelor, care intră în responsabilitatea aplicației mobile. Sistemul de misiuni cu \textit{geofencing} notifică când turistul intră în raza unui obiectiv și pune interacțiunea în bufferul local.

\subsection{Serviciul client de învățare federată}

Serviciul client este implementat în Dart, în \texttt{fl\_client\_service.dart}, fără alte biblioteci adiționale, deoarece dimensiunea redusă a modelului, de doar 1.281 de parametri ($\approx$5 KB), nu necesită adăugarea unui \textit{runtime} de 1,3-30 MB, cum am argumentat în capitolul anterior. Cu scopul de a reutiliza cod, același \textit{forward pass} (metoda \texttt{predictScore}) este folosit și pentru a calcula scorul de potrivire afișat sub formă de categorie - scăzut, mediu, ridicat - atunci când utilizatorul apasă pe un obiectiv pentru a deschide fișa acestuia.

Antrenarea locală cuprinde 5 epoci de SGD pe ce se află în buffer, cu o rată de învățare egală cu 0,01. La fiecare eșantion, după ce se calculează gradienții pentru cele 3 straturi, norma L2 a vectorului acestora este limitată la pragul $\tau=1$ prin rescalare, fiind vorba de limitarea descrisă la 4.4.4, și, în plus, actualizările cu gradienți NaN sau Inf sunt ignorate, ceea ce previne propagarea valorilor nenumerice spre modelul global. Partea de \textit{backpropagation} se regăsește în metoda \texttt{\_trainLocally} din fișierul \texttt{fl\_client\_service.dart}.

\subsection{Adăugarea de zgomot pentru asigurarea confidențialității\\diferențiale}

Mecanismul DP este implementat în metoda \texttt{\_addDPNoise}, metodă care este apelată pe dispozitiv imediat după ce are loc antrenarea local, înainte să fie trimisă actualizarea spre server. Se calculează, mai întâi, delta - diferența dintre ponderile antrenate și varianta modelului de când runda a început. Apoi, fiecare coordonată a deltei este trunchiată la intervalul $[-\tau, \tau]$ cu $\tau=1$, pentru a mărgini sensibilitatea. După care, peste fiecare coordonată se adaugă zgomot Gaussian $\mathcal{N}(0,\,\sigma^2)$ cu $\sigma = \sigma_{\mathrm{noise}} \cdot \tau = 0{,}1$, ponderile trimise către server fiind reconstruite adăugând delta cu zgomotul aplicat la varianta globală, astfel încât serverul nu primește niciodată delta în stare curată.

Zgomotul Gaussian este generat prin transformata Box-Muller\footnote{Box, G. E. P., \& Muller, M. E. (1958). A note on the generation of random normal deviates. \textit{The Annals of Mathematical Statistics}, 29(2), 610-611.}, implementarea putându-se observa în metoda \texttt{\_gaussian}. Transformata produce eșantioane $\mathcal{N}(0,\,\sigma^2)$ din două variabile uniforme independente $U_1, U_2 \sim \mathcal{U}(0,1)$, iar valoarea $U_1$ este trunchiată la $10^{-10}$ pentru a evita $\log(0)$, dar trunchierile de la acest pas nu trebuie confundate cu limitarea normei L2 de la secțiunea anterioară, deoarece aceea scalează vectorul întreg al gradienților ca să prevină fenomenul de \textit{exploding gradients}, pe când cea de la pasul de DP ține sensibilitatea mărginită pentru ca zgomotul adăugat să aducă un efect real asupra confidențialității. Implementarea celor două metode, \texttt{\_addDPNoise} și \texttt{\_gaussian}, este atașată în \textbf{\textit{listing-ul}~\ref{lst:dpnoise}} din Anexe.

\subsection{Bufferul local de interacțiuni și persistența datelor}

Interacțiunile dintre runde ale utilizatorului ajung într-un buffer persistent prin \texttt{Shared-\\Preferences} (\path{local_data_service.dart}), ele fiind indexate după ID-ul obiectivului turistic sau cultural. Există și un risc, adunarea unor intrări duplicate, așa că, prin structura \texttt{\_LandmarkBuffer} (\texttt{fl\_client\_service.dart}) se păstrează o singură intrare pentru fiecare obiectiv. Sunt prezente două cazuri pentru acordarea unei etichete de \textit{engagement} (scor) unui obiectiv, primul fiind acordarea unei evaluări sub formă de recenzie (1-5 stele), care suprascrie orice scor anterior atribuit locației. Pentru celelalte tipuri de interacțiuni, se păstrează cel mai ridicat scor înregistrat pentru obiectiv. De asemenea, persistența prin \texttt{SharedPreferences} face ca bufferul să nu fie golit dacă aplicația este oprită înainte ca runda să se încheie - este golit doar după trimiterea cu succes a actualizării spre server. În plus, ecranul de profil afișează numărul curent de interacțiuni din buffer, în acest mod utilizatorul având vizibilitate asupra stării procesului FL local, după cum se poate observa în \textbf{figura~\ref{fig:profile-fl-status}} din Anexe.

\subsection{Stocarea temporară a obiectivelor pentru funcționarea offline}

Lista completă de obiective se salvează tot în \texttt{SharedPreferences} (\path{local_data_service.dart}), sub o cheie globală, care este comună tuturor utilizatorilor, deoarece obiectivele sunt date publice, deci nu este nevoie să individualizăm pentru fiecare utilizator. Acestea nu au un timp de expirare predefinit, locațiile fiind suprascrise la fiecare \textit{fetch} reușit. Fluxul legat de interograrea obiectivelor este reprezentat prin metoda \texttt{fetchAllLandmarks} (\texttt{map\_provider.dart}) care solicită prin punctul de acces \path{/landmarks/all} toate locațiile de la server, iar dacă se întoarce o eroare, se încarcă colecția de obiective din \textit{cache}. Întreaga colecție este stocată unitar, și nu este implementată paginarea bazată pe locația curentă. Aceasta este o limitare care poate deveni o problemă la scară largă, unde un număr foarte mare de obiective ar face randarea pe hartă imposibilă.

\subsection{Algoritmul de generare a rutelor}

În generarea unei rute este inclus pentru utilizator un panou inferior care expune un parametru configurabil (rata de departajare; 0-1000 metri, implicit 200 de metri), panoul fiind un \textit{slider} între „Shortest path" și „Most relevant". La apăsarea butonului de generare a rutei, se trimite de la client un \textit{request} de tip POST către \path{/routes/generate} cu: poziția curentă, numărul maxim de opriri (5), timpul disponibil (300 de minute) și valoarea \textit{slider-ului}. Interesele utilizatorului nu sunt trimise în \textit{request}, fiindcă serverul le preia din profilul utilizatorului din baza de date.

Pe server, obiectivele de pe o rază de 10 km sunt ordonate după un scor inițial $\textit{scor}_{\mathrm{FL}} \times 0.8 + \textit{scor}_{\mathrm{distance}} \times 0.2$, unde scorul FL provine din predicția MLP, iar scorul legat de distanță normalizează distanța față de poziția curentă în intervalul $[0, 1]$. Dacă modelul FL nu este disponibil în Redis pentru a fi folosit la acest pas, scorul de potrivire a intereselor înlocuiește componenta FL cu același coeficient de 80\%. Se construiește o rută cu obiectivele selectate în maniera \textit{greedy nearest-neighbour}, cu rata de configurare (\texttt{flTiebreakerM}) menționată anterior - oprirea următoare se determină minimizând $\textit{dist} - \textit{flTiebreakerM} \times \textit{interest\_match}$, unde $\textit{interest\_match}$ se calculează din interesele utilizatorului - ajungându-se la următorul rezultat: un obiectiv mai relevant poate fi ales în locul unuia mai apropiat, dacă se încadrează în raza \textit{slider-ului} setată în panou. Durata atribuită fiecărei opriri pornește dintr-o valoare aleasă empiric în funcție de tipul obiectivului, și este scalată prin scorul FL al obiectivului în intervalul $[0.75, 1.25]$, astfel că locațiile cu un scor mai mare primesc mai mult timp de vizitare. Modul în care arată o rută generată poate fi vizualizat în \textbf{figura~\ref{fig:route-generation}} din Anexe.

\subsection{Sistemul de misiuni și tehnologia de \textit{geofencing}}

Misiunile se accesează din fișa unui obiectiv, prin butonul „View quests". La apăsare, aplicația măsoară, pe baza poziției curente, cât de departe se află utilizatorul de obiectiv, iar în cazul în care utilizatorul se află la mai mult de 100 de metri, misiunile apar doar în modul citire, fără posibilitatea de a trimite un răspuns pentru ele. Sub acest prag, misiunea devine activă: utilizatorul răspunde la un \textit{quiz} (întrebare cu variante sau notă text), trimis printr-o cerere HTTP de tip POST la \path{/quests/{id}/complete}. Pentru un răspuns corect, serverul acordă puncte și misiunea se marchează drept finalizată pe dispozitiv. Cât despre unul greșit, obiectivul se marchează, totuși, ca vizitat, de vreme ce utilizatorul este fizic acolo. Panoul de misiuni poate fi observat în \textbf{figura~\ref{fig:proximity-sheet}} din Anexe.

Detectarea apropierii față de locații se face printr-un serviciu de \textit{geofencing} manual (\path{proximity_service.dart}), construit pe fluxul de poziții GPS cu un filtru de 10 metri, ceea ce înseamnă că fluxul GPS trimite o actualizare doar dacă utilizatorul s-a deplasat cu cel puțin 10 metri față de poziția curentă. Cu fiecare actualizare, se recalculează distanța față de obiectivele din zona curentă - cele din raza de 1500 de metri față de ultima poziție. Evenimentul ENTER se trimite când utilizatorul ajunge la mai puțin de 100 de metri de un obiectiv, iar cel de EXIT când iese din această rază. Dacă utilizatorul s-a deplasat cu mai mult de 200 de metri față de ultimul punct de la care s-a actualizat lista de obiective, aceasta se reîncarcă de la server.

\section{Implementarea serviciilor \textit{backend}}

Din punct de vedere al serviciilor \textit{backend}, în aplicația CultureQuest există trei arii principale. Prima este reprezentată de modul în care gestionează serverul agregarea FL și cuprinde: primirea actualizărilor de la clienți, aplicarea \textit{staleness discount-ului}, limitarea deltei și coordonarea prin Redis pentru a preveni problemele de sincronizare. A doua arie cuprinde punctele de acces API asociate comunicării dispozitivelor mobile cu serverul, printre care: descărcarea modelului global de către clienți, interogarea statusului procesului FL, și alte puncte de acces necesare pentru interfața de comunicare. A treia și ultima arie descrisă în această secțiune este alcătuită din sistemul de recenzii și moderarea conținutului. Aici sunt incluse: agregarea evaluărilor asociate unui obiectiv, moderarea automată prin OpenAI Moderation API cu \textit{fallback} pe o listă locală de cuvinte interzise, dar și un sistem de revizuire manuală pentru obiective, povești și \textit{quest-uri} concepute de turiști sau localnici.

\subsection{Serviciul de agregare}

Protocolul FL din CultureQuest este asincron, unde există doar o actualizare în cadrul unei runde (și nu există barieră de sincronizare), iar agregarea se face printr-o medie ponderată cu discount de vechime. La primirea unei actualizări de la un client, serverul calculează delta față de ponderile globale și limitează norma L2 a acesteia la $\tau = 1$. Implementarea se regăsește în metoda \texttt{clipped\_avg} aflată în \textbf{\textit{listing-ul}~\ref{lst:clippedfedavg}} din Anexe. Dacă un client trimite o actualizare cu normă mare, delta se scalează înainte de agregare. Astfel, contribuția sa este limitată la cel mult $\frac{1}{N_{\mathrm{virtual}}}$ pentru fiecare coordonată de pondere. Aici se adaugă și discountul de vechime, care taie suplimentar contribuția clienților care antrenează pe o versiune veche a modelului global. Numărul efectiv de eșantioane devine $n_{\mathrm{eff}} = \lfloor n \cdot \frac{1}{1+s} \rfloor$, unde $s$ arată numărul de runde cu care clientul rămâne în urmă, și, mai mult, modelul global intră în propria sa agregare cu cel puțin de patru ori mai multe eșantioane virtuale decât aduce clientul, ceea ce împiedică ca o singură actualizare să schimbe radical ponderile globale.

Pentru a preveni problemele de sincronizare când se trimit actualizări în același timp de la mai mulți clienți, ciclul de citire-agregare-scriere este securizat din punct de vedere al condițiilor de cursă (\textit{race conditions}) printr-un \textit{lock} Redis cu token-ul \texttt{SET NX PX}, ce are un TTL (\textit{Time to Live}) de 5 secunde care previne blocaje în cazul unei erori, pentru că în cazul în care \textit{lock-ul} nu poate fi obținut după zece încercări la un interval de 0,3 secunde distanță, cererea este respinsă. Eliberarea lui se face printr-un script Lua care verifică în mod atomic că token-ul stocat este cel al procesului curent, pentru a evita ștergerea accidentală a unui \textit{lock} care aparține unui alt client.

\subsection{Punctele de acces API}

Între dispozitivele mobile și serviciul FL din \textit{backend}, comunicarea se realizează prin patru puncte de acces HTTP, pe care le-am descris sumar în \textbf{tabelul~\ref{tab:fl-endpoints}}, putându-se observa că doar un punct de acces este rezervat conturilor de tip administrator, cel care permite încărcarea unui model pre-antrenat și resetarea istoricului de runde. Alte rute nelegate de partea de FL se găsesc în \textbf{tabelul~\ref{tab:all-endpoints}} din Anexe.

\begin{table}[h]
\centering
\scriptsize
\caption{Punctele de acces HTTP ale serviciului FL.}
\label{tab:fl-endpoints}
\begin{tabular}{|l|p{3cm}|p{5.5cm}|p{2.5cm}|}
\hline
\textbf{Metodă} & \textbf{Cale} & \textbf{Scop} & \textbf{Autorizare} \\
\hline
GET & \texttt{/model/global} & Descărcare model global și primire rundă curentă & Publică \\
\hline
POST & \texttt{/model/update} & Trimitere actualizare locală după antrenare & JWT utilizator \\
\hline
POST & \texttt{/admin/initialize} & Încărcare ponderi pre-antrenate, resetare istoric & JWT administrator \\
\hline
GET & \texttt{/model/status} & Status FL: rundă, clienți unici, eșantioane agregate & Publică \\
\hline
\end{tabular}
\end{table}

\subsection{Sistemul de recenzii și moderarea conținutului}

Recenziile au două părți (text liber și evaluare cu stele de la 1 la 5) și sunt moderate la trimitere. Dacă este definit în cod un \textit{API key} pentru OpenAI, serverul face un apel către OpenAI Moderation API\footnote{OpenAI Moderation API - documentație oficială. \url{https://platform.openai.com/docs/guides/moderation} (accesat 19.06.2026).}, care returnează un câmp \texttt{flagged} și categoriile pentru care textul recenziei devine \textit{flagged}. Totuși, dacă cheia nu este prezentă sau cererea către API returnează o eroare, se recurge la un \textit{fallback} bazat pe un filtru local de expresii și cuvinte care cuprind insulte și discurs de ură. În orice caz, recenziile semnalate nu sunt vizibile altor utilizatori și trebuie revizuite de administrator.

Poveștile și \textit{quest-urile} propuse de utilizatori ajung într-o listă cu statusul \textit{pending}, devenind vizibile tuturor utilizatorilor autentificați, și există două metode prin care se aprobă acestea, ori prin intermediul unui administrator, ori prin strângerea a cinci voturi din comunitate de la turiști sau localnici diferiți de autor. Adăugarea unor obiective noi pe hartă se face, însă, doar prin aprobarea unui administrator.

\section{Fluxul etapei de pre-antrenare}

Pentru etapa de pre-antrenare a modelului global, se folosesc patru seturi de date publice - TSMC2014 \cite{yang2015tsmc} (\textit{check-in-uri} din New York și Tokyo), TIST2015 \cite{yang2016tist} (\textit{check-in-uri} la nivel global), ubicomp2013 \cite{yang2013ubicomp} (\textit{check-in-uri} și recomandări pentru cei care vizitează restaurante din New York) și Yelp \cite{yelp2021dataset} (recenzii cu evaluări de la 1 la 5 stele pentru afaceri locale, în special din SUA și Canada) - la care se adaugă și un set de exemple sintetice, necesare deoarece sursele reale prezintă doar interacțiuni cu obiective deschise, un utilizator care ajunge la un muzeu închis în acel moment având probabilitate foarte mică de a scrie o recenzie sau de a rezolva \textit{quest-urile} asociate. Astfel, fără exemple sintetice, modelul nu ar vedea niciodată intrări cu caracteristica \texttt{isOpen} egală cu 0 în etapa de antrenare. Seturile TSMC2014, TIST2015 și ubicomp2013 conțin date de tip LBSN (\textit{Location-Based Social Network}), fiind înregistrări anonimizate de pe Foursquare\footnote{Foursquare. \url{https://foursquare.com/company/who-we-are/} (accesat 19.06.2026).} - o aplicație de \textit{check-in} social unde utilizatorii se înregistrau manual când ajungeau la o locație (restaurant, muzeu, parc etc.). Datele sunt relevante pentru că arată locații vizitate în mod voluntar, ceea ce reprezintă un semnal indirect de preferință și un sentiment pozitiv față de obiectiv.

Fiecare înregistrare din surse este transformată într-un vector de caracteristici de 22 de dimensiuni, iar eticheta (scorul) de \textit{engagement} se deduce din tipul interacțiunii. Interacțiunile din seturile Foursquare primesc eticheta de bază 0,70 (check-in simplu), crescând la 0,75 pentru recomandări scrise, respectiv progresiv până la 0,90 în cazul vizitelor repetate, pe când recenziile din setul Yelp preiau direct scorul normalizat (numărul de stele împărțit la 5). Exemplele sintetice cu obiective închise primesc eticheta 0,10. Distribuția seturilor de date poate fi vizualizată în \textbf{figura~\ref{fig:dataset-overview}}.

Antrenarea propriu-zisă rulează cel mult 60 de epoci cu \textit{early stopping} dacă timp de 10 epoci nu apare nicio îmbunătățire pe setul de validare. Se folosește SGD cu rata de învățare inițială de 0,01 și un factor de reducere de 0,98 per epocă, pe \textit{batch-uri} de 256 de exemple. Funcția de pierdere utilizată este MSE, la fel și ulterior pe dispozitiv în antrenarea FL. Însă, rezultatele pre-antrenării au arătat o problemă de regresie către medie: modelul prezice preponderent valori în intervalul 0,70-0,75, cu puține valori sub 0,40, din cauza dezechilibrului dintre exemplele cu scor mare și cele cu scor mic. \textit{Fine-tuning-ul} rezolvă această problemă prin două intervenții aplicate setului de antrenare: adăugarea a 2.000 de exemple sintetice suplimentare cu scor mic per tip de obiectiv (față de 300 la pre-antrenare) și suprareprezentarea de trei ori a înregistrărilor cu scorul sub 0,40. În paralel, rata de învățare crește la 0,02 cu un factor de reducere mai lent (0,995) pe parcursul a 40 de epoci, pentru a permite modelului să iasă din platoul atins fără să își suprascrie cunoștințele deja acumulate.

\begin{figure}[!h]
\centering
\hspace*{-0.5cm}
\includegraphics[width=1.05\textwidth]{pics/charts/dataset_overview.png}
\caption{Prezentarea seturilor de date utilizate la pre-antrenare.}
\label{fig:dataset-overview}
\end{figure}

\section{\textit{Framework-ul} pentru simularea procesului de învățare federată}

Simularea rulează 600 de runde FL cu un \textit{pool} de 10.000 de clienți eterogeni, 80\% dintre aceștia fiind clienți recenți, cu o vechime de doar 0-2 runde față de modelul global. Restul sunt clienți tardivi, cu o vechime între 15-30 de runde. Nu inactivitatea dispozitivelor este cea care cauzează vechimea, ci latența rețelei sau faptul că mai mulți clienți antrenează în paralel. Până ajunge actualizarea unui client la server, există riscul ca modelul global să fi avansat deja cu câteva runde prin intermediul contribuțiilor celorlalți. Simularea reproduce acest decalaj prin două mecanisme, odată prin pornirea antrenării de la un \textit{snapshot} al modelului global de acum $s$ runde, nu de la versiunea curentă, dar și prin perturbarea prin inversare a etichetelor de \textit{engagement} locale, într-o proporție de 70\%, astfel încât gradienții săi să contracareze parțial direcția curentă de antrenare. Actualizările fiecărui client sunt ponderate cu discountul de vechime $\frac{1}{1+s}$ înainte de agregare (unde $s$ reprezintă numărul de runde cu care clientul este în urmă față de modelul global). Influența clienților tardivi este, prin urmare, limitată, fără a-i exclude complet din procesul federat.

Antrenarea locală folosește MSE, la fel ca și pe dispozitiv, însă calitatea modelului global este urmărită, la nivelul serverului, cu BCE. BCE penalizează mai sever predicțiile sigure, dar greșite, ceea ce o face mai potrivită pentru a surprinde degradarea unui model cu ieșire de tip \textit{sigmoid}. Parametrii simulării sunt centralizați în \textbf{tabelul~\ref{tab:simulare-fl}}, iar în ceea ce privește corectitudinea componentelor de agregare și a formulei de discount, aceasta este verificată printr-o suită de 20 de teste \textit{pytest}, care acoperă \texttt{build\_initial\_weights}, \texttt{fl\_predict}, \texttt{avg}, \texttt{clipped\_avg} și formula de discount.

\begin{table}[h]
\centering
\scriptsize
\caption{Parametrii simulării procesului FL.}
\label{tab:simulare-fl}
\begin{tabular}{|p{4.5cm}|c|p{7.5cm}|}
\hline
\textbf{Parametru} & \textbf{Valoare} & \textbf{Descriere} \\
\hline
\texttt{N\_CLIENTS} & 10.000 & Numărul de clienți simulați. \\
\hline
\texttt{N\_ROUNDS} & 600 & Numărul de runde FL. \\
\hline
\texttt{INTERACTIONS\_PER\_ROUND} & 15 & Interacțiuni locale pentru o rundă. \\
\hline
\texttt{LEARNING\_RATE} & 0,01 & Rata de învățare locală (SGD). \\
\hline
\texttt{LOCAL\_EPOCHS} & 5 & Epoci de antrenare locală per rundă. \\
\hline
\texttt{DP\_SIGMA} & 0,1 & Deviația standard a zgomotului Gaussian (DP). \\
\hline
\texttt{FRESH\_PROB} & 0,8 & Proporția clienților recenți (vechime 0-2 runde). \\
\hline
\texttt{FRESH\_STALENESS} & (0, 2) & Intervalul de vechime pentru clienții recenți (runde). \\
\hline
\texttt{STALE\_STALENESS} & (15, 30) & Intervalul de vechime pentru clienții tardivi (runde). \\
\hline
\texttt{STALE\_LABEL\_FLIP\_RATE} & 0,7 & Rata de inversare a etichetelor pentru clienții tardivi. \\
\hline
\end{tabular}
\end{table}

\section{Dificultăți întâmpinate în procesul de implementare}

Implementarea componentelor descrise în acest capitol a adus mai multe provocări tehnice, rezolvate iterativ, iar cele mai semnificative sunt enumerate mai jos:

\begin{itemize}
\item \textbf{Regresia modelului pre-antrenat către medie.} Indiferent de vectorul de intrare, modelul prezice valori între 0,7-0,75 - egale cu media setului de antrenare - din cauza dezechilibrului dintre exemple cu scor mare și cele cu scor mic, iar soluția a constat în folosirea tehnicii de \textit{fine-tuning} cu suprareprezentare de trei ori a înregistrărilor cu scorul sub 0,40 și extinderea setului de exemple sintetice de la 300 la 2.000 per tip de obiectiv.

\item \textbf{Absența datelor cu obiective care nu sunt deschise la momentul vizitării în sursele reale.} Interacțiunile din seturile Foursquare și Yelp corespund exclusiv obiectivelor deschise, și niciun utilizator nu interacționează cu un obiectiv la care nu a putut intra. Fără exemple sintetice cu caracteristica \texttt{isOpen=0}, modelul nu ar fi întâlnit niciodată această situație, rezultând în scoruri ridicate și pentru obiective inaccesibile.

\item \textbf{Echilibrarea discountului de vechime.} Formula $\frac{1}{1+s}$ a fost aleasă deoarece descrește ușor și nu exclude în totalitate clienții tardivi. Excluderea directă a clienților cu vechime mare ar elimina contribuții valide de la turiști sau localnici cu rețea lentă.

\item \textbf{Concurența la agregarea asincronă.} FL-ul asincron permite primirea simultană a mai multor actualizări, ceea ce poate conduce la probleme de sincronizare în ciclul \textit{read-aggregate-write} al modelului global. Soluția constă într-un \textit{lock} Redis eliberat atomic printr-un script Lua.
\end{itemize}

\chapter{Evaluare}

% TODO: paragraf de prezentare a capitolului

\section{Corectitudinea funcțională}

\section{Performanța modelului de învățare federată}

\subsection{Rezultatele etapei de pre-antrenare}

\subsection{Rezultatele simulării procesului de învățare federată}

\section{Evaluarea confidențialității, robusteții și scalabilității}

\section{Analiză comparativă și discuții}

% TODO: rezumat capitol + tranziție spre Capitolul 7

\chapter{Concluzii}

% TODO: paragraf de prezentare a capitolului

\section{Concluzii generale}

\section{Direcții de cercetare și lucrări viitoare}

\clearpage
\phantomsection
\addcontentsline{toc}{chapter}{Bibliografie}

% Asa se specifica folosirea unui fisier cu referinte bibliografice:
\bibliographystyle{plainurl}
\bibliography{bibliography}

%% O alta varianta ar fi fost includerea de articole direct in acest fisier
%% in felul urmator:
%% \begin{thebibliography}{ABC}
%%
%% \bibitem{article}
%%  H. Baali, H. Djelouat, A. Amira and F. Bensaali,
%%  ``Empowering Technology Enabled Care Using IoT and Smart Devices:
%   A Review''. In: IEEE Sensors Journal, vol. 322 (10), pp. 891--921, 1905.
%%
%% (more \bibitem items here...)
%%
%% \end{thebibliography}

%% Daca vreti ca o sectiune sa inceapa pe o pagina noua, puteti forta acest lucru cu comanda "\newpage", ca mai jos:

%\newpage

\chapter*{Anexe}\addcontentsline{toc}{chapter}{Anexe}

\begin{appendices}

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{pics/db.pdf}
\caption{Modelul complet de date al aplicației CultureQuest: colecțiile MongoDB, câmpurile principale și relațiile dintre ele.}
\label{fig:db}
\end{figure}

\clearpage
\begin{figure}[!h]
\centering
\includegraphics[width=0.3\textwidth]{pics/profile-fl-status.png}
\caption{Ecranul de profil cu statusul FL și numărul de interacțiuni din buffer.}
\label{fig:profile-fl-status}
\end{figure}

\begin{figure}[!h]
\centering
\includegraphics[width=0.3\textwidth]{pics/route-generation.png}
\caption{Ruta generată pe hartă.}
\label{fig:route-generation}
\end{figure}

\clearpage
\begin{figure}[h]
\centering
\includegraphics[width=0.3\textwidth]{pics/proximity-sheet.png}
\caption{Panoul de misiuni (\textit{quest-uri}) disponibil la detectarea unui obiectiv în apropiere.}
\label{fig:proximity-sheet}
\end{figure}

{\scriptsize
\begin{longtable}{|p{1.1cm}|p{5.5cm}|p{4.5cm}|p{1cm}|}
\caption{Toate punctele de acces REST ale aplicației CultureQuest (prefixul \texttt{/api} este omis).} \label{tab:all-endpoints} \\
\hline
\textbf{Met.} & \textbf{Cale} & \textbf{Scop} & \textbf{Auth} \\
\hline
\multicolumn{4}{|l|}{\textit{{-} = publică \quad U = JWT utilizator \quad A = JWT administrator}} \\
\hline
\endfirsthead
\hline
\textbf{Met.} & \textbf{Cale} & \textbf{Scop} & \textbf{Auth} \\
\hline
\endhead
POST & \texttt{/auth/register} & Înregistrare cont & - \\
\hline
POST & \texttt{/auth/login} & Autentificare & - \\
\hline
GET & \texttt{/auth/me} & Profil utilizator & U \\
\hline
PATCH & \texttt{/auth/interests} & Actualizare interese & U \\
\hline
GET & \texttt{/landmarks/} & Obiective din apropiere & - \\
\hline
GET & \texttt{/landmarks/all} & Toate obiectivele & - \\
\hline
GET & \texttt{/landmarks/\{id\}} & Detalii obiectiv & - \\
\hline
POST & \texttt{/landmarks/} & Creare obiectiv & U \\
\hline
PATCH & \texttt{/landmarks/\{id\}/website} & Actualizare website & U \\
\hline
PATCH & \texttt{/landmarks/\{id\}/info} & Actualizare info & U \\
\hline
POST & \texttt{/landmarks/\{id\}/visit} & Contor vizite & - \\
\hline
POST & \texttt{/landmarks/\{id\}/rate} & Evaluare obiectiv & - \\
\hline
POST & \texttt{/landmarks/submit} & Propunere obiectiv nou & U \\
\hline
POST & \texttt{/landmarks/\{id\}/stories} & Adăugare poveste & U \\
\hline
GET & \texttt{/landmarks/admin/pending} & Obiective în așteptare & A \\
\hline
GET & \texttt{/landmarks/admin/submissions} & Propuneri utilizatori & A \\
\hline
GET & \texttt{/landmarks/admin/pending-stories} & Povești în așteptare & A \\
\hline
POST & \texttt{/landmarks/admin/\{id\}/approve} & Aprobare obiectiv & A \\
\hline
POST & \texttt{/landmarks/admin/\{id\}/reject} & Respingere obiectiv & A \\
\hline
PATCH & \texttt{/landmarks/admin/\{id\}} & Editare obiectiv & A \\
\hline
DELETE & \texttt{/landmarks/admin/\{id\}} & Ștergere obiectiv & A \\
\hline
POST & \texttt{/landmarks/admin/stories/\{id\}/approve} & Aprobare poveste & A \\
\hline
POST & \texttt{/landmarks/admin/stories/\{id\}/reject} & Respingere poveste & A \\
\hline
GET & \texttt{/routes/global} & Rute globale & - \\
\hline
POST & \texttt{/routes/admin} & Creare rută manuală & A \\
\hline
POST & \texttt{/routes/generate} & Generare rută FL & U \\
\hline
GET & \texttt{/quests/} & Lista misiuni & U \\
\hline
GET & \texttt{/quests/me/progress} & Progres misiuni & U \\
\hline
POST & \texttt{/quests/\{id\}/complete} & Completare misiune & U \\
\hline
POST & \texttt{/quests/submit/\{landmark\_id\}} & Propunere misiune & U \\
\hline
GET & \texttt{/quests/admin/pending} & Misiuni în așteptare & A \\
\hline
POST & \texttt{/quests/admin/\{id\}/approve} & Aprobare misiune & A \\
\hline
POST & \texttt{/quests/admin/\{id\}/reject} & Respingere misiune & A \\
\hline
GET & \texttt{/federated/model/global} & Model global FL & - \\
\hline
POST & \texttt{/federated/model/update} & Trimitere actualizare FL & U \\
\hline
POST & \texttt{/federated/admin/initialize} & Inițializare model FL & A \\
\hline
GET & \texttt{/federated/model/status} & Status FL & - \\
\hline
GET & \texttt{/users/leaderboard} & Clasament & U \\
\hline
POST & \texttt{/users/me/reset-progress} & Resetare progres & U \\
\hline
POST & \texttt{/users/location} & Actualizare locație & U \\
\hline
GET & \texttt{/users/nearby} & Utilizatori din apropiere & U \\
\hline
POST & \texttt{/events/} & Creare eveniment & U \\
\hline
GET & \texttt{/events/landmark/\{id\}} & Evenimente obiectiv & - \\
\hline
GET & \texttt{/events/nearby} & Evenimente din apropiere & - \\
\hline
PATCH & \texttt{/events/\{id\}} & Actualizare eveniment & U \\
\hline
DELETE & \texttt{/events/\{id\}} & Ștergere eveniment & U \\
\hline
POST & \texttt{/community/vote/story/\{id\}} & Vot poveste & U \\
\hline
POST & \texttt{/community/vote/quest/\{id\}} & Vot misiune & U \\
\hline
GET & \texttt{/community/pending} & Conținut în așteptare & U \\
\hline
POST & \texttt{/comments/\{landmark\_id\}} & Adăugare recenzie & U \\
\hline
PATCH & \texttt{/comments/\{comment\_id\}} & Actualizare recenzie & U \\
\hline
GET & \texttt{/comments/\{landmark\_id\}} & Recenzii obiectiv & - \\
\hline
GET & \texttt{/comments/admin/flagged} & Recenzii semnalate & A \\
\hline
POST & \texttt{/comments/admin/\{id\}/approve} & Aprobare recenzie & A \\
\hline
DELETE & \texttt{/comments/admin/\{id\}} & Ștergere recenzie & A \\
\hline
\end{longtable}
}

\chapter{Extrase de Cod} % Introduce o nouă anexă

\begin{lstlisting}[caption={Adăugarea zgomotului Gaussian pentru fiecare coordonată a deltei (\texttt{fl\_client\_service.dart:348-373}).}, label={lst:dpnoise}]
void _addDPNoise(List<List<double>> globalSnapshot) {
  final noiseStd = kDPSigma * kDPClipNorm;
  for (int l = 0; l < _weights.length; l++) {
    final delta = List<double>.generate(
      _weights[l].length,
      (i) => (_weights[l][i] - globalSnapshot[l][i])
                  .clamp(-kDPClipNorm, kDPClipNorm),
    );
    for (int i = 0; i < delta.length; i++) {
      delta[i] += _gaussian(noiseStd);
    }
    for (int i = 0; i < _weights[l].length; i++) {
      _weights[l][i] = globalSnapshot[l][i] + delta[i];
    }
  }
}

double _gaussian(double sigma) {
  final u1 = _rng.nextDouble().clamp(1e-10, 1.0);
  final u2 = _rng.nextDouble();
  final z = math.sqrt(-2.0 * math.log(u1))
    * math.cos(2.0 * math.pi * u2);
  return z * sigma;
}
\end{lstlisting}

\clearpage
\begin{lstlisting}[caption={Agregare cu limitarea normei deltei (\texttt{backend/app/federated/model.py:72-100}).}, label={lst:clippedfedavg}, language=Python]
def clipped_avg(
    updates: list[tuple[int, list[list[float]]]],
    global_weights: list[list[float]],
    max_norm: float = 1.0,
) -> list[list[float]]:
    clipped: list[tuple[int, list[list[float]]]] = []
    for n_samples, client_weights in updates:
        delta = [
            [c - g for c, g in zip(cl, gl)]
            for cl, gl in zip(client_weights, global_weights)
        ]
        norm = math.sqrt(sum(v ** 2 for layer in delta for v in layer))
        scale = min(1.0, max_norm / max(norm, 1e-8))
        clipped_weights = [
            [g + d * scale for g, d in zip(gl, dl)]
            for gl, dl in zip(global_weights, delta)
        ]
        clipped.append((n_samples, clipped_weights))
    return avg(clipped)
\end{lstlisting}

\end{appendices}
\end{document}